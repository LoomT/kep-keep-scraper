✻ Conversation compacted (ctrl+o for history)

  ⎿  Referenced file loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt
  ⎿  Read loader\src\main\kotlin\dev\cse3000\loader\SqlWriter.kt (241 lines)
  ⎿  Read loader\db-schema.sql (125 lines)
  ⎿  Read loader\src\main\kotlin\dev\cse3000\loader\Main.kt (70 lines)
  ⎿  Read loader\src\main\kotlin\dev\cse3000\loader\SchemaModel.kt (115 lines)
  ⎿  Plan file referenced (~\.claude\plans\i-want-to-make-mellow-reddy.md)

● Loader is in a clean state — last run produced 2 RelatedProposal rows and 3622
   Comments (incl. 29 from review threads), no FK drops, no missing-status
  warnings. Ready for the next instruction.

✻ Brewed for 6m 0s

❯ all pr comments in the same pr should be linked to one another in             
  chronological order, not just direct replies, just like issue comments. Also  
  take care of meta field "authors" in addition to "author" and "proposal       
  author". Authors field should have multiple authors separated by commas.      

  Read 1 file (ctrl+o to expand)

● Two edits: (1) chain all PR review comments chronologically per PR (like issue
   comments), and (2) handle plural authors meta field.

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 31 lines, removed 36 lines
      507      }
      508  
      509      /**
      510 -     * PR code-review threads (line comments on diffs). Source
          -: `keep-pr-review-comments.jsonl`,                            
      511 -     * one comment per line with `id`, optional `in_reply_to_i
          -d`, `_pr`, `user.login`,                                      
      512 -     * `created_at`, `body`. Replies link to a parent in the s
          -ame PR via `in_reply_to_id`;                                  
      513 -     * roots have `in_reply_to_id == null`.                   
      510 +     * PR code-review comments (line comments on diffs). Sourc
          +e: `keep-pr-review-comments.jsonl`,                           
      511 +     * one comment per line with `id`, `_pr`, `user.login`, `c
          +reated_at`, `body`.                                           
      512       *
      515 -     * Parent always precedes child in time (GitHub guarantees
          - `in_reply_to_id` references an                               
      516 -     * earlier same-PR comment), so a created_at sort gives us
          - a topologically valid order:                                 
      517 -     * by the time we visit a reply, its parent's allocated co
          -mment_id is already in                                        
      518 -     * `ghIdToAllocated`. Threads are kept distinct in the Com
          -ment table by chaining                                        
      519 -     * `commentOnCommentId` to the parent's allocated id; `nul
          -l` for roots so they show as                                  
      520 -     * top-level comments on the proposal.                    
      513 +     * Within each PR, every comment is chained chronologicall
          +y: the earliest comment is the                                
      514 +     * root (`commentOnCommentId = null`) and every subsequent
          + comment points at its                                        
      515 +     * predecessor by `created_at`. Same shape as `mapIssueCom
          +ments`, just keyed off the PR's                               
      516 +     * review-comment stream. The `in_reply_to_id` GitHub fiel
          +d is ignored — we keep one chain                              
      517 +     * per PR rather than per review thread.                  
      518       */
      519      private fun mapPrReviewThreadComments(proposalIds: Set<Int
           >): List<Comment> {
      520          val byPr = readJsonlObjects(normalizedDir, "keep-pr-re
           view-comments")
     ...
      524  
      525          return byPr.flatMap { (prNumber, prComments) ->
      526              val proposalId = prNumber.toString().padStart(4, '
           0')
      530 -            val ghIdToAllocated = mutableMapOf<Long, Long>()  
      531 -            prComments.sortedBy { it.getJsonString("created_at
          -") }                                                          
      532 -                .mapNotNull { json ->                         
      533 -                    val ghId = json["id"]!!.jsonPrimitive.long
      534 -                    val parentGhId = json["in_reply_to_id"]?.j
          -sonPrimitive?.longOrNull                                      
      535 -                    val parentAllocated = parentGhId?.let { gh
          -IdToAllocated[it] }                                           
      536 -                    if (parentGhId != null && parentAllocated 
          -== null) {                                                    
      537 -                        log.warn(                             
      538 -                            "Dropping review comment {} in PR 
          -#{}: in_reply_to_id={} not in same PR's data",                
      539 -                            ghId, prNumber, parentGhId,       
      540 -                        )                                     
      541 -                        return@mapNotNull null                
      542 -                    }                                         
      543 -                    val authorLogin = json["user"]!!.jsonObjec
          -t.getJsonString("login")                                      
      544 -                    val allocated = commentIds.nextId()       
      545 -                    ghIdToAllocated[ghId] = allocated         
      546 -                    Comment(                                  
      547 -                        commentId = allocated,                
      548 -                        authorId = resolveGHLogin(authorLogin)
          -,                                                             
      549 -                        projectId = projectId,                
      550 -                        proposalId = proposalId,              
      551 -                        commentOnCommentId = parentAllocated, 
      552 -                        createdAt = json.getJsonString("create
          -d_at"),                                                       
      553 -                        content = json.getJsonStringOrNull("bo
          -dy").orEmpty(),                                               
      554 -                    )                                         
      555 -                }                                             
      527 +            val sorted = prComments.sortedBy { it.getJsonStrin
          +g("created_at") }                                             
      528 +            if (sorted.isEmpty()) return@flatMap emptyList()  
      529 +                                                              
      530 +            val rootJson = sorted.first()                     
      531 +            val rootComment = Comment(                        
      532 +                commentId = commentIds.nextId(),              
      533 +                authorId = resolveGHLogin(rootJson["user"]!!.j
          +sonObject.getJsonString("login")),                            
      534 +                projectId = projectId,                        
      535 +                proposalId = proposalId,                      
      536 +                commentOnCommentId = null,                    
      537 +                createdAt = rootJson.getJsonString("created_at
          +"),                                                           
      538 +                content = rootJson.getJsonStringOrNull("body")
          +.orEmpty(),                                                   
      539 +            )                                                 
      540 +            sorted.drop(1).runningFold(rootComment) { previous
          +, json ->                                                     
      541 +                Comment(                                      
      542 +                    commentId = commentIds.nextId(),          
      543 +                    authorId = resolveGHLogin(json["user"]!!.j
          +sonObject.getJsonString("login")),                            
      544 +                    projectId = projectId,                    
      545 +                    proposalId = proposalId,                  
      546 +                    commentOnCommentId = previous.commentId,  
      547 +                    createdAt = json.getJsonString("created_at
          +"),                                                           
      548 +                    content = json.getJsonStringOrNull("body")
          +.orEmpty(),                                                   
      549 +                )                                             
      550 +            }                                                 
      551          }
      552      }
      553  

● Now handle the plural authors meta field. Need to update the data class, the
  metadata parser, the warning log site, and the buildList:

  Read 1 file (ctrl+o to expand)

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 2 lines, removed 1 line
      258          val trimmedContent: String,
      259          val title: String,
      260          val topic: String?, // called Type in KEEPs
      261 -        val author: String?,                                  
      261 +        /** Comma-separated list of author names. Sourced from
          + `Author`, `Authors`, or `Proposal Author` meta fields (whiche
          +ver are present, joined). */                                  
      262 +        val authors: String?,                                 
      263          val contributors: String?,
      264          val rawStatus: String,
      265          val normalizedStatus: String,

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 11 lines, removed 4 lines
      354  
      355          if (metaMap["type"] == null) log.warn("Type field miss
           ing in {}", metaWithTitle)
      356  
      357 -        val authors = listOfNotNull(metaMap["author"], metaMap
          -["proposal author"])                                          
      358 -        assert(authors.size <= 1)                             
      359 -        val author = authors.singleOrNull()                   
      360 -        if (author == null) log.warn("Author field missing in 
          -{}", metaWithTitle)                                           
      357 +        // Authors can come from any of three meta keys: singu
          +lar `Author`, plural `Authors`                                
      358 +        // (comma-separated list), or `Proposal Author`. If mu
          +ltiple are present we join them                               
      359 +        // with `, ` so the consumer (the `ProposalRevisionAut
          +hor` builder) can split on commas                             
      360 +        // uniformly.                                         
      361 +        val authorEntries = listOfNotNull(                    
      362 +            metaMap["author"],                                
      363 +            metaMap["authors"],                               
      364 +            metaMap["proposal author"],                       
      365 +        )                                                     
      366 +        val authors = authorEntries.joinToString(", ").ifBlank
          + { null }                                                     
      367 +        if (authors == null) log.warn("Author field missing in
          + {}", metaWithTitle)                                          
      368  
      369          if (metaMap["status"] == null) log.warn("Status field 
           missing in {}", metaWithTitle)
      370  

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 1 line, removed 1 line
      375              proposalTextWithoutMetaData,
      376              title,
      377              metaMap["type"],
      378 -            author,                                           
      378 +            authors,                                          
      379              metaMap["contributors"] ?: metaMap["proposal contr
           ibutors"],
      380              rawStatus = rawStatus,
      381              normalizedStatus = normalizeStatus(statusToken),

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 1 line, removed 1 line
      140              }
      141              val topic = topics.distinct().single()
      142  
      143 -            val authorNames = proposalCommits.second.mapNotNul
          -l { it.proposalData.author }                                  
      143 +            val authorNames = proposalCommits.second.mapNotNul
          +l { it.proposalData.authors }                                 
      144              if (authorNames.isEmpty()) log.warn("Author is mis
           sing in $proposalId")
      145  
      146              val proposal = Proposal(

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 8 lines, removed 4 lines
      180  
      181              // ProposalRevisionAuthor is a many-to-many: every
            ProposalRevision row gets one
      182              // ProposalRevisionAuthor row per attributed autho
           r. Authors are the union of the
      183 -            // KEEP `Author` field plus the `Contributors` lis
          -t (comma-separated names),                                    
      184 -            // de-duplicated within each revision so the compo
          -site PK                                                       
      185 -            // (project_id, proposal_id, revision_index, autho
          -r_id) doesn't collide.                                        
      183 +            // KEEP `Author`/`Authors`/`Proposal Author` field
          +s plus the `Contributors` list,                               
      184 +            // both treated as comma-separated lists, de-dupli
          +cated within each revision so the                             
      185 +            // composite PK (project_id, proposal_id, revision
          +_index, author_id) doesn't collide.                           
      186              val proposalAuthorRevisions = proposalCommits.seco
           nd.flatMap { proposalCommit ->
      187                  val rawAuthors = buildList {
      188 -                    proposalCommit.proposalData.author?.let { 
          -add(it) }                                                     
      188 +                    proposalCommit.proposalData.authors       
      189 +                        ?.split(',')                          
      190 +                        ?.map { it.trim() }                   
      191 +                        ?.filter { it.isNotBlank() }          
      192 +                        ?.forEach { add(it) }                 
      193                      proposalCommit.proposalData.contributors
      194                          ?.split(',')
      195                          ?.map { it.trim() }
● Run the loader to verify:

● Bash(./gradlew :loader:run -PkeepProjectId=3 -PkepProjectId=4 2>&1 | tail -20)
  ⎿  * **Author**: Nikita Bobko
     * **Contributors**: Alejandro Serrano, Marat Akhin, Mikhail Zarechensk
     ii, Pavel Kunyavskiy, Roman Venediktov
     … +74 lines (ctrl+o to expand)
  ⎿  (timeout 3m)

● Let me spot-check the new comment chaining for one PR:

  Searched for 2 patterns (ctrl+o to expand)

● Comment chains look correct: each PR/issue's first comment has NULL parent,
  followed by a chronological chain. Build succeeded with:

  - ProposalRevisionAuthor=1330 (was 1220, +110): proposals using the plural
  Authors: meta key (e.g. "Rich Errors" with two authors, "KMP Publication
  Scheme", "Sliding window extensions", "Char-to-code conversions") now
  contribute their authors. The "Author field missing in …" WARN lines for those
   four proposals are gone.
  - Person=547 / names=94 (was 545 / 92): two new authors enrolled from those
  proposals.
  - Comment=3732 unchanged: same set of review comments, just now linearly
  chained per-PR by created_at rather than per-thread by in_reply_to_id.
  - RelatedProposal=2 unchanged.
  - Remaining "Author is missing" WARNs for 0071, 0073, 0075, 0107 are genuinely
   authorless proposals.

✻ Churned for 2m 41s

❯ you should assert that only one author related field is in a proposal. Drop   
  the contributors field mapping.                                               

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 9 lines, removed 10 lines
      358  
      359          if (metaMap["type"] == null) log.warn("Type field miss
           ing in {}", metaWithTitle)
      360  
      361 -        // Authors can come from any of three meta keys: singu
          -lar `Author`, plural `Authors`                                
      362 -        // (comma-separated list), or `Proposal Author`. If mu
          -ltiple are present we join them                               
      363 -        // with `, ` so the consumer (the `ProposalRevisionAut
          -hor` builder) can split on commas                             
      364 -        // uniformly.                                         
      365 -        val authorEntries = listOfNotNull(                    
      366 -            metaMap["author"],                                
      367 -            metaMap["authors"],                               
      368 -            metaMap["proposal author"],                       
      369 -        )                                                     
      370 -        val authors = authorEntries.joinToString(", ").ifBlank
          - { null }                                                     
      361 +        // Authors come from exactly one of the three meta key
          +s: singular `Author`, plural                                  
      362 +        // `Authors` (comma-separated list), or `Proposal Auth
          +or`. Two of them appearing in the                             
      363 +        // same proposal is almost certainly a mistake (which 
          +one is canonical?), so we assert                              
      364 +        // mutual exclusivity rather than silently merging.   
      365 +        val authorKeysPresent = listOf("author", "authors", "p
          +roposal author").filter { metaMap[it] != null }               
      366 +        assert(authorKeysPresent.size <= 1) {                 
      367 +            "Multiple author-related fields $authorKeysPresent
          + in $metaWithTitle"                                           
      368 +        }                                                     
      369 +        val authors = authorKeysPresent.firstOrNull()?.let { m
          +etaMap[it] }                                                  
      370          if (authors == null) log.warn("Author field missing in
            {}", metaWithTitle)
      371  
      372          if (metaMap["status"] == null) log.warn("Status field 
           missing in {}", metaWithTitle)

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 1 line, removed 2 lines
      262          val trimmedContent: String,
      263          val title: String,
      264          val topic: String?, // called Type in KEEPs
      265 -        /** Comma-separated list of author names. Sourced from
          - `Author`, `Authors`, or `Proposal Author` meta fields (whiche
          -ver are present, joined). */                                  
      265 +        /** Comma-separated list of author names. Sourced from
          + exactly one of `Author`, `Authors`, or `Proposal Author` meta
          + fields. */                                                   
      266          val authors: String?,
      267 -        val contributors: String?,                            
      267          val rawStatus: String,
      268          val normalizedStatus: String,
      269          val implementedAt: String?,

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Removed 1 line
      378              title,
      379              metaMap["type"],
      380              authors,
      381 -            metaMap["contributors"] ?: metaMap["proposal contr
          -ibutors"],                                                    
      381              rawStatus = rawStatus,
      382              normalizedStatus = normalizeStatus(statusToken),
      383              implementedAt,

● Update(loader\src\main\kotlin\dev\cse3000\loader\KeepMapper.kt)
  ⎿  Added 9 lines, removed 16 lines
      179                  }
      180  
      181              // ProposalRevisionAuthor is a many-to-many: every
            ProposalRevision row gets one
      182 -            // ProposalRevisionAuthor row per attributed autho
          -r. Authors are the union of the                               
      183 -            // KEEP `Author`/`Authors`/`Proposal Author` field
          -s plus the `Contributors` list,                               
      184 -            // both treated as comma-separated lists, de-dupli
          -cated within each revision so the                             
      185 -            // composite PK (project_id, proposal_id, revision
          -_index, author_id) doesn't collide.                           
      182 +            // ProposalRevisionAuthor row per attributed autho
          +r. Authors come from the KEEP                                 
      183 +            // `Author`/`Authors`/`Proposal Author` field (tre
          +ated as a comma-separated list),                              
      184 +            // de-duplicated within each revision so the compo
          +site PK                                                       
      185 +            // (project_id, proposal_id, revision_index, autho
          +r_id) doesn't collide.                                        
      186              val proposalAuthorRevisions = proposalCommits.seco
           nd.flatMap { proposalCommit ->
      187 -                val rawAuthors = buildList {                  
      188 -                    proposalCommit.proposalData.authors       
      189 -                        ?.split(',')                          
      190 -                        ?.map { it.trim() }                   
      191 -                        ?.filter { it.isNotBlank() }          
      192 -                        ?.forEach { add(it) }                 
      193 -                    proposalCommit.proposalData.contributors  
      194 -                        ?.split(',')                          
      195 -                        ?.map { it.trim() }                   
      196 -                        ?.filter { it.isNotBlank() }          
      197 -                        ?.forEach { add(it) }                 
      198 -                }                                             
      187 +                val rawAuthors = proposalCommit.proposalData.a
          +uthors                                                        
      188 +                    ?.split(',')                              
      189 +                    ?.map { it.trim() }                       
      190 +                    ?.filter { it.isNotBlank() }              
      191 +                    .orEmpty()                                
      192                  rawAuthors.distinct().map { name ->
      193                      ProposalRevisionAuthor(
      194                          projectId = projectId,

● Bash(./gradlew :loader:run -PkeepProjectId=3 -PkepProjectId=4 2>&1 | tail -10)
  ⎿  14:28:11.502 WARN  d.c.loader.KeepMapper - Skipping discussion #455: b
     ody does not contain a https://github.com/Kotlin/KEEP/blob/main/propos
     als/KEEP-... link
     … +62 lines (ctrl+o to expand)
  ⎿  (timeout 3m)

● Build green, mutual-exclusivity assertion holds across all 108 proposals.
  Counts:

  - ProposalRevisionAuthor=345 (was 1330): drops the contributor rows; only
  attributed authors remain.
  - Person=503 / names=50 (was 547 / 94): contributor-only names no longer
  enrolled as Persons.
  - PersonUsername=453 unchanged (those are GH logins, not names).
  - Comment=3732, RelatedProposal=2 unchanged.

✻ Churned for 55s

❯ This whole project is expected to be placed in a folder of a monorepo in      
  gitlab, where each folder is reserved to each contributor's project. This     
  monorepo root should have the db-schema.sql and maybe the shared sqlite       
  database file. what are your recommendations to migrate without while still   
  being able to open just this porject while ignoring other folders but on      
  occasion interacting with the parent directory?                               

● Let me explore the project layout and current path assumptions before
  planning.

● Explore(Survey project layout and path assumptions)
  ⎿  Done (25 tool uses · 44.9k tokens · 1m 31s)
  (ctrl+o to expand)

  Read 7 files (ctrl+o to expand)

● Now I'll append the new addendum to the plan file:

● Updated plan
  ⎿  /plan to preview
  ⎿  Tool use rejected

● Updating the plan with two course corrections: folder name stays
  CSE3000_RQ3-KEEP-KEP (matching rootProject.name), and the monorepo uses git
  submodules.
● Updated plan
  ⎿  /plan to preview

● Updated plan
  ⎿  /plan to preview

● Updated plan
  ⎿  /plan to preview

● Updated plan
  ⎿  /plan to preview

● Updated plan
  ⎿  /plan to preview

● User approved Claude's plan
  ⎿  Plan saved to: ~\.claude\plans\i-want-to-make-mellow-reddy.md · /plan to 
     edit
     Plan: GitHub scraper for Kotlin/KEEP and kubernetes/enhancements

     Context

     This is a TU Delft CSE3000 research project (RQ3) doing a comparative study
      of two
     ecosystem proposal processes:

     - KEEP — Kotlin Evolution and Enhancement Process. Hosted at Kotlin/KEEP.
     Proposals are markdown files merged via PR, but discussion happens 
     primarily
     on the repo's GitHub Discussions tab (GraphQL-only API).
     - KEP — Kubernetes Enhancement Proposals. Hosted at
     kubernetes/enhancements.
     Each proposal is a directory under keps/ containing a kep.yaml, README, and
     PRR docs, with a tracking issue and discussion spread across multiple PRs 
     and
     issues (REST works fine).

     The goal of this scraper is to download everything that might be needed for
     both repos in one pass, persist it locally for offline processing, and
     support
     incremental refresh via a Gradle task without re-downloading what hasn't
     changed (since the GitHub API rate limit is a concern). The final analysis
     target is a MySQL database with a schema TBD, so the scraper output should
     be structured but loosely-coupled to any specific schema (JSON-on-disk is
     the
     right level of abstraction). The :rq3 analysis module is out of scope here.

     The project is currently a clean Gradle multi-module scaffold (Kotlin
     2.3.20, JVM
     toolchain 25). All src/main/kotlin directories are empty. Existing version
     catalog already provides kotlinx-coroutines 1.10.2,
     kotlinx-serialization-json
     1.9.0, and kotlinx-datetime 0.7.1.

     Approach

     Hybrid HTTP layer — Ktor Client for REST + a hand-written GraphQL escape
     hatch in the same Ktor stack. No Apollo, no second HTTP framework. Reasons:

     - For ~500 proposals total, REST + ETags is simpler, gives self-documenting
     JSON-on-disk artifacts (good for a thesis appendix), and supports cheap
     incremental reruns via If-None-Match.
     - KEEP's GitHub Discussions tab forces some GraphQL. We add a single
     runGraphQL(query, variables) method to the Ktor client and write the few
     queries we need by hand (repository.discussions, discussion.comments,
     discussion.comments.replies, reactions). Apollo's codegen + schema tooling
     is overkill for ~3-4 queries.
     - KEP's discussions are issue/PR comments → pure REST.

     Two-tier disk persistence under data/ (gitignored):

     data/
       cache/
         raw/<repo>/<endpoint>/<id>.json     # verbatim API responses
         etags.json                          # url -> etag
         last-sync.json                      # endpoint -> max(updated_at)
       normalized/
         keep-proposals.jsonl, keep-comments.jsonl, keep-reviews.jsonl,
         keep-discussions.jsonl, keep-discussion-comments.jsonl,
         kep-proposals.jsonl, kep-comments.jsonl, kep-reviews.jsonl,
         kep-yaml.jsonl, users.jsonl, reactions.jsonl, labels.jsonl,
         timeline-events.jsonl
       manifests/<run-timestamp>.json        # rate-limit usage, counts, commit
     SHA

     JSONL is later loaded into MySQL with a small bulk-insert step once the
     schema
     is finalized. Skip SQLite/SQLDelight — adds schema-migration burden with no
     queryability gain at this volume.

     Module layout

     :utils (domain-agnostic, package dev.cse3000.gh)

     - client/GithubClient.kt — Ktor wrapper. Suspend functions per endpoint:
     listPullRequests, getPullRequest, listIssueComments, listReviewComments,
     listReviews, listReactions, listTimelineEvents, listChecks, getContent,
     runGraphQL. Uses ktor-client-cio + content-negotiation + kotlinx-json.
     - client/RateLimiter.kt — Semaphore(8) for concurrency cap + adaptive
     throttle
     reading X-RateLimit-Remaining / X-RateLimit-Reset; suspends until reset
     when remaining drops below a configurable floor (default 100).
     - client/RetryPolicy.kt — handles 403 + Retry-After (secondary rate limit)
     and 5xx with exponential backoff; treats 304 Not Modified as a cache hit.
     - client/Paginator.kt — Flow<JsonElement> over Link header pagination.
     - cache/EtagStore.kt — load/save etags.json, attach If-None-Match per URL.
     - cache/RawCache.kt — write/read JSON keyed by URL → file path.
     - cache/SyncCursor.kt — load/save last-sync.json per endpoint key.
     - io/JsonlWriter.kt — append-only JSONL output.
     - io/Env.kt — read GITHUB_TOKEN (fail fast if missing).
     - io/RunManifest.kt — emit run-summary JSON at end of each scrape.
     - model/ — generic GitHub DTOs (GhUser, GhIssue, GhPullRequest,
     GhComment, GhReview, GhReaction, GhLabel, GhTimelineEvent,
     GhCheckRun, GhDiscussion, GhDiscussionComment, RateLimitInfo).
     These map directly to GitHub responses and are shared by both scrapers.
     - scraper/ProposalScraper.kt — interface: suspend fun run(); fun 
     discover(): Flow<ProposalRef>.

     :utils/build.gradle.kts already has the kotlinx bundle. Add Ktor + plugin:
     alias(libs.plugins.kotlinPluginSerialization) and Ktor dependencies.

     :keep (package dev.cse3000.keep)

     - model/KeepProposal.kt, model/KeepProposalRef.kt, model/KeepDiscussion.kt
     - scrape/KeepProposalDiscovery.kt — list PRs that touch proposals/*.md;
     resolve linked issue via the PR's "closing issues" GraphQL field with REST
     body-parse fallback (Fixes #N).
     - scrape/KeepDiscussionsDiscovery.kt — GraphQL query
     repository(owner:"Kotlin", name:"KEEP") { discussions(first:100, 
     after:$cursor) { ... } } to enumerate discussions, then fetch comments +
     replies + reactions.
     - scrape/KeepScraper.kt — orchestrator wired into Main.kt CLI.
     - Main.kt — CLI entry (Gradle application plugin). Subcommands full and
     update (incremental).

     :kep (package dev.cse3000.kep)

     - model/KepProposal.kt, model/KepYaml.kt, model/KepProposalRef.kt
     - parse/KepYamlParser.kt — uses kaml (kotlinx-serialization-yaml port)
     for kep.yaml.
     - scrape/KepDiscovery.kt — walks keps/ via the contents API, parses each
     kep.yaml, resolves tracking issue + linked PRs from the yaml metadata.
     - scrape/KepScraper.kt — orchestrator wired into Main.kt CLI.

     :keep and :kep both need alias(libs.plugins.kotlinPluginSerialization)
     applied (compiler plugin, doesn't transit through
     implementation(projects.utils)).

     Auth, rate-limit, incremental refresh

     - GITHUB_TOKEN env var → Authorization: Bearer <token> (gives 5000 req/hr
     REST,
     5000 points/hr GraphQL).
     - Concurrency: Semaphore(8) ceiling on in-flight requests.
     - After every response: read X-RateLimit-Remaining; if < 100, suspend until
     X-RateLimit-Reset.
     - Conditional GET: EtagStore attaches If-None-Match; 304 → return cached
     body
     for free (does not consume quota).
     - Incremental: since query param on issues/comments lists, persisted per
     endpoint in last-sync.json. Combined with ETags, a refresh after a quiet
     day
     costs only a few hundred requests.

     Gradle wiring

     - :keep and :kep apply the Gradle application plugin so each can be run
     via ./gradlew :keep:run and ./gradlew :kep:run.
     - Add a top-level Gradle task updateScrapedData that depends on
     :keep:run --args="update" and :kep:run --args="update". This is the
     one-command incremental refresh the user asked for.
     - Add a separate fullScrape task for the initial backfill.
     - Both tasks fail fast if GITHUB_TOKEN is unset.

     Version catalog additions (gradle/libs.versions.toml)

     [versions]
     ktor = "3.3.1"
     kaml = "0.99.0"

     [libraries]
     ktorClientCore           = { module = "io.ktor:ktor-client-core",
           version.ref = "ktor" }
     ktorClientCio            = { module = "io.ktor:ktor-client-cio",
           version.ref = "ktor" }
     ktorClientContentNeg     = { module =
     "io.ktor:ktor-client-content-negotiation", version.ref = "ktor" }
     ktorClientLogging        = { module = "io.ktor:ktor-client-logging",
           version.ref = "ktor" }
     ktorClientAuth           = { module = "io.ktor:ktor-client-auth",
           version.ref = "ktor" }
     ktorSerializationJson    = { module =
     "io.ktor:ktor-serialization-kotlinx-json", version.ref = "ktor" }
     kaml                     = { module = "com.charleskorn.kaml:kaml",
           version.ref = "kaml" }

     [bundles]
     ktorClient = ["ktorClientCore", "ktorClientCio", "ktorClientContentNeg",
                   "ktorClientLogging", "ktorClientAuth",
     "ktorSerializationJson"]

     (Verify exact latest stable Ktor and kaml versions at implementation time —
     these are placeholders pinned to known-good releases.)

     Critical files to be modified or created

     - gradle/libs.versions.toml — add Ktor, kaml entries
     - utils/build.gradle.kts — apply serialization plugin, add Ktor bundle
     - keep/build.gradle.kts — apply serialization + application plugin
     - kep/build.gradle.kts — apply serialization + application plugin, add kaml
     - .gitignore — add data/, .env
     - New: utils/src/main/kotlin/dev/cse3000/gh/**/*.kt (per layout above)
     - New: keep/src/main/kotlin/dev/cse3000/keep/**/*.kt
     - New: kep/src/main/kotlin/dev/cse3000/kep/**/*.kt
     - New: top-level build.gradle.kts aggregator tasks (updateScrapedData,
     fullScrape) — currently no root build.gradle.kts exists

     Verification

     1. ./gradlew build — compiles cleanly across all modules.
     2. GITHUB_TOKEN=<pat> ./gradlew :keep:run --args="full --limit=5" — fetches
     5 KEEP proposals end-to-end; verify data/cache/raw/Kotlin/KEEP/... and
     data/normalized/keep-proposals.jsonl populate. Inspect a sample JSONL line
     for completeness.
     3. GITHUB_TOKEN=<pat> ./gradlew :kep:run --args="full --limit=5" — same for
      KEP;
     verify kep.yaml parsing produced sensible kep-yaml.jsonl rows.
     4. Re-run the same command immediately. Expect the run manifest to show a
     high 304-rate (most requests served from ETag cache) and zero new
     normalized
     rows. This proves incremental refresh works.
     5. ./gradlew updateScrapedData — runs both incremental updates; manifest
     shows
     since cursor advanced and quota consumed is minimal.
     6. Spot-check a known proposal: e.g. KEEP-87 (context receivers) and
     keps/sig-node/2008-forensic-container-checkpointing for KEP. Confirm the
     discussion thread / comments are captured fully.
     7. Unit-test KepYamlParser against 3-4 real kep.yaml files committed to
     kep/src/test/resources/.

     Open items deferred to implementation

     - Final Ktor/kaml version pins — check Maven Central at implementation
     time.
     - MySQL schema — supplied by the user later; the JSONL output is
     deliberately
     loosely structured so the loader can be written when the schema is known.
     - Whether to store proposal markdown bodies inside the JSONL or as separate
     files under data/cache/raw/<repo>/blobs/. Recommend the latter for
     cleanliness
     if bodies are large.

     ---
     Addendum: wire up periodic flushing + use kotlin.time.Duration everywhere

     Context

     Two related cleanups:

     1. JsonlWriter#flush was added speculatively during implementation but
     never
     called. For a multi-hour scrape that gets Ctrl-C'd (or hits kill -9),
     several minutes of buffered JSONL lines and the latest ETag/cursor state
     can currently be lost. We want to flush all in-flight state to disk every
     60s so a kill loses at most ~60s of progress.
     2. Time values in the codebase are a mix: some sites use raw Long
     milliseconds (retryDelayMs(): Long, delay(60_000)), some have been
     linter-touched into (...).milliseconds. Standardize on
     kotlin.time.Duration everywhere a time value is constructed, returned,
     stored, or passed across a function boundary — only convert to/from
     primitive seconds/ms at the I/O edge (HTTP header parsing, epoch math).

     Approach — periodic flush

     A periodic-flush coroutine launched from each Main.kt. It persists the ETag
     store, sync cursor, and all open JSONL writers on a fixed cadence, then is
     explicitly cancelled in the finally block before the run manifest is
     written.

     The flush body itself runs under withContext(NonCancellable) so
     cancellation
     during the actual disk write doesn't leave the ETag/cursor JSON files in a
     partially-written state (atomic-rename in EtagStore.persist /
     SyncCursor.persist already handles process-kill mid-write, but
     NonCancellable keeps the cooperative-cancellation path clean).

     Approach — Duration policy

     - Function signatures that represent a time value take/return
     kotlin.time.Duration, never Long ms or Long sec.
     - Constants are constructed via the extension properties:
     60.seconds, 30.seconds, 500.milliseconds, etc. — not
     60_000.milliseconds or (60 * 1000).milliseconds.
     - HTTP header values (Retry-After, X-RateLimit-Reset) are parsed as
     Long (their wire format) and immediately wrapped: headerSec.seconds.
     - Epoch arithmetic (System.currentTimeMillis() / 1000 vs an epoch
     reset stamp) stays in Long; only the resulting delta is converted to
     Duration before being returned to a caller or passed to delay.
     - delay(Duration) is preferred over delay(Long) so cancellation behavior
     and units are unambiguous at the call site.

     Critical files to be modified

     - utils/src/main/kotlin/dev/cse3000/gh/io/JsonlSink.kt
       - Add suspend fun flushAll() that iterates writers.values and calls each
     JsonlWriter.flush() under runCatching so a single bad writer doesn't
     abort the rest. Existing JsonlWriter.flush() is already correctly
     Mutex-guarded — no change there.
     - utils/src/main/kotlin/dev/cse3000/gh/client/GithubClient.kt
       - Rename retryDelayMs(resp): Long → retryDelay(resp): Duration.
     Body becomes:
       resp.headers[HttpHeaders.RetryAfter]?.toLongOrNull()?.let {
         return it.seconds + 500.milliseconds
     }
     resp.headers["X-RateLimit-Reset"]?.toLongOrNull()?.let { resetAt ->
         val waitSec = resetAt - System.currentTimeMillis() / 1000
         if (waitSec > 0) return (waitSec + 1).seconds
     }
     return 60.seconds
       - Replace inline (1000L * (1L shl ...)).milliseconds backoff with
     (1L shl (attempt - 1).coerceAtMost(5)).seconds.
       - All delay(...) calls take a Duration.
     - utils/src/main/kotlin/dev/cse3000/gh/client/RateLimiter.kt
       - Rewrite the wait calculation as
     val wait = (resetAtEpochSec.get() - nowSec + 1).seconds and
     delay(wait) — drops the manual * 1000 and the .milliseconds
     coercion the linter added.
     - keep/src/main/kotlin/dev/cse3000/keep/Main.kt
       - Inside the runBlocking { ... }, before the try block, launch a child
     coroutine:
       val flushInterval = 60.seconds
     val flushJob = launch {
         while (isActive) {
             delay(flushInterval)
             withContext(NonCancellable) {
                 runCatching { ctx.etags.persist() }
                 runCatching { ctx.cursor.persist() }
                 runCatching { ctx.sink.flushAll() }
             }
         }
     }
       - In the finally block, before RunManifestWriter.write(...):
       flushJob.cancel()
     flushJob.join()
     - kep/src/main/kotlin/dev/cse3000/kep/Main.kt
       - Identical change to the KEP Main.kt.

     Why not a single shared helper

     The two Main.kt files each own their own runBlocking scope and
     ScrapeContext.
     A shared helper would require either passing the scope around (awkward) or
     hiding the lifecycle inside ScrapeContext. The duplicated ~12-line block is
     not worth abstracting; if a third scraper appears, that's the right time to
     factor it out.

     Verification

     1. ./gradlew build — compiles cleanly. No deprecation warnings about
     delay(Long) or implicit unit conversions.
     2. Spot-grep: Grep "delay\(\d|Long.*ms|millis = " should find no remaining
     raw-Long time-passing call sites in :utils, :keep, :kep.
     3. GITHUB_TOKEN=... ./gradlew :keep:run --args="full --limit=20" and watch
     the
     log: flush is silent on success; retry/back-off log lines now show
     Duration's toString() form (e.g. 2s, 500ms) instead of bare numbers.
     4. Crash-recovery test: start a scrape, wait ~90s, Ctrl-C the process.
     Verify that data/cache/etags.json and data/cache/last-sync.json are
     non-empty and parseable, and that data/normalized/*.jsonl contains
     complete JSONL lines (no trailing partial line). Re-run with update mode
     and confirm via the run manifest's requests_304 count that the cached
     state was honored.
     5. Confirm normal-completion path still emits a coherent run manifest with
     no
     "flushJob still running" leak (i.e. process exits within a second of the
     final phase finishing).

     ---
     Addendum: replace "∞" denominators with discovered totals

     Context

     Progress logs in IssueAndPrCollector and KeepDiscussionsCollector currently
     display processed/limit ?: "∞", which shows 25/∞ on a full scrape because
     the total isn't known. We want a real denominator so the user can estimate
     how long a run will take and what fraction of the work is done.

     Approach

     Three sources, three different mechanisms — pick whichever is cheapest per
     endpoint:

     1. REST list endpoints (/repos/{slug}/issues, /repos/{slug}/pulls):
     GitHub returns a Link: <...&page=N>; rel="last" header on the first
     paginated response. Probing with ?per_page=1 makes N exactly equal to
     the total item count. Costs 1 extra request per phase per run; subsequent
     runs serve the probe from ETag cache (304, no quota cost).
     2. GraphQL discussions connection: has a built-in totalCount field —
     free, just an extra leaf on the existing query.
     3. KEP yaml discovery: total is already known in-memory after the tree
     filter (kepYamlEntries.size), already logged correctly. No change.

     Caveat for /pulls in incremental mode

     The /repos/{slug}/pulls endpoint does not support a since query
     parameter, so the probe returns all-time PR count regardless of cursor. The
     collector then client-side filters out PRs older than sinceCursor. The
     honest log message in that case is "of N total in repo" so it's clear the
     denominator is pre-filter.

     /issues does honor since, so the probe gives the post-filter total
     directly — the denominator is meaningful.

     Denominator policy in the progress log

     denominator = limit ?: total ?: "?"

     i.e. respect --limit if set; otherwise show the discovered total; otherwise
     fall back to "?" only if the probe genuinely failed (no Link header).

     Critical files to be modified

     - utils/src/main/kotlin/dev/cse3000/gh/client/Paginator.kt
       - Add a regex + helper fun lastPageNumber(linkHeader: String?): Int?
     matching <...&page=(\d+)...>; rel="last".
     - utils/src/main/kotlin/dev/cse3000/gh/client/GithubClient.kt
       - Add a public suspend fun countListEndpoint(path: String, query: 
     Map<String, String> = emptyMap()): Int? that overrides per_page=1 /
     page=1 in the query, calls the existing private fetchWithRetry(url)
     (which already returns the Link header in its Pair<JsonElement, String?>),
     and returns Paginator.lastPageNumber(link) ?: 1 when the
     response had at least one item but no rel="last" header (single-page
     result), else 0 for an empty list. Returns null only on probe failure.
     - utils/src/main/kotlin/dev/cse3000/gh/scraper/IssueAndPrCollector.kt
       - In collectIssues: before launching the paginated collect, call
     client.countListEndpoint("/repos/$slug/issues", params) and store as
     total. Compute denominator = limit ?: total ?: "?". Use it in the
     phase-start log and progress logs. The since is already in params so
     the probe respects it.
       - In collectPullRequests: same pattern, but the start-of-phase log labels
     the figure as "of {} total in repo (since-filter applied client-side)"
     when incremental is true and a sinceCursor exists, so the user knows
     the denominator includes pre-filter items.
     - keep/src/main/kotlin/dev/cse3000/keep/KeepDiscussionsCollector.kt
       - Add totalCount to the discussions(...) selection in the
     LIST_DISCUSSIONS GraphQL query.
       - Read it on the first page response and store as total. Compute the
     same denominator and use it in the phase-start log and progress logs.
     Prefix with "of {} total" / "of {} updated since cursor" depending on
     whether sinceCursor was set (totalCount is repo-wide, not since-aware).

     Verification

     1. ./gradlew build — green.
     2. GITHUB_TOKEN=... ./gradlew :keep:run --args="full --limit=5" — confirm
     logs read e.g. Issues progress: 25/5 (limit denominator) and
     Discussions progress: 50/342 (real GraphQL totalCount).
     3. Drop --limit and confirm progress reads 25/N where N is the real
     total — not ∞.
     4. Re-run incremental and confirm the probe response is served from ETag
     cache (run manifest's requests_304 count includes the probe).

     ---
     Addendum: graceful cancellation of the scraping Gradle tasks

     Context

     Today, hitting Ctrl-C during ./gradlew :keep:run (or the aggregator
     fullScrape / updateScrapedData tasks) causes the JVM to exit before the
     finally blocks finish: the run manifest never gets written, ctx.persist()
     in the success path is skipped, and the JSONL writers may have buffered
     bytes that are lost. The only thing currently saving us is the 60s periodic
     flush — anything emitted in the last 0–60s of the run is lost on a kill.

     We want Ctrl-C (and any other JVM shutdown signal) to:

     1. Stop launching new HTTP / git work,
     2. Let in-flight per-item work complete or unwind cleanly,
     3. Persist etags, cursor, gitCursor, and JsonlSink to disk,
     4. Write a run manifest reflecting that the run was cancelled, and
     5. Exit non-zero (or zero, as long as the manifest is on disk).

     A bounded shutdown timeout (default 30s) keeps a stuck network call from
     preventing exit forever — a second Ctrl-C still hard-kills.

     Approach

     JVM shutdown hook + cooperative coroutine cancellation, leveraging the
     structure already in place:

     1. The two Main.kt files already wrap everything in runBlocking { ... }
     with a try/catch/finally + a flushJob child coroutine. The root
     runBlocking job already covers all collectors via structured concurrency.
     2. Register a JVM shutdown hook that:
       - Calls rootJob.cancel(CancellationException("Shutdown signal 
     received")),
       - Then runBlocking { withTimeoutOrNull(30.seconds) { rootJob.join() } }
     so the hook thread blocks until cleanup completes (or the timeout fires).
     3. The existing finally block is the cleanup path. It must work under
     cancellation, so:
       - All suspending cleanup (ctx.persist(), flushJob.join(), manifest
     write) is wrapped in withContext(NonCancellable) { ... }.
       - The finally also removes the shutdown hook (so a normal-exit path
     doesn't leave a dangling hook).
     4. Catch CancellationException in main's try/catch to set a cancelled
     flag; do NOT rethrow it (we want main to exit cleanly with a written
     manifest, not propagate a stack trace). Other exceptions are still
     rethrown.
     5. Two small atomicity tweaks elsewhere so cancellation can't leave
     half-written state on disk:
       - JsonlWriter.append: build line + lineSeparator() once and do a
     single writer.write(...) call inside the mutex. Removes the
     write-then-newLine race where cancellation between the two calls
     produces a JSONL line with no newline.
       - GithubClient.fetchWithRetry's 200-success branch: wrap
     cache.put(url, text) + etags.put(url, etag) in
     withContext(NonCancellable) so an ETag is never recorded for a body
     that didn't make it to the cache (would cause a future 304 with no
     cached body — already an error() in the 304 branch).

     Critical files to be modified

     - utils/src/main/kotlin/dev/cse3000/gh/io/RunManifest.kt
       - Add val cancelled: Boolean = false to the RunManifest data class
     (with @SerialName("cancelled") for consistency).
     - utils/src/main/kotlin/dev/cse3000/gh/io/JsonlWriter.kt
       - Replace the writer.write(line); writer.newLine() pair with a single
     writer.write(line + System.lineSeparator()) so the on-disk state is
     always a complete line or no line at all.
     - utils/src/main/kotlin/dev/cse3000/gh/client/GithubClient.kt
       - Wrap the cache-put + etag-put pair in the 2xx branch in
     withContext(NonCancellable) { ... }. Add import 
     kotlinx.coroutines.NonCancellable
     and import kotlinx.coroutines.withContext (already imported via wildcard).
     - keep/src/main/kotlin/dev/cse3000/keep/Main.kt and
     kep/src/main/kotlin/dev/cse3000/kep/Main.kt
       - Capture val rootJob = coroutineContext.job at the top of runBlocking.
       - Build a JVM shutdown hook (see snippet below) and register it via
     Runtime.getRuntime().addShutdownHook(...).
       - Add var cancelled = false and a dedicated catch (ce: 
     CancellationException)
     branch that sets cancelled = true, logs at WARN, and falls through (no
     rethrow).
       - In the finally, wrap flushJob.join() + ctx.persist() + manifest write
           - ctx.close() in withContext(NonCancellable) { ... }.
       - Pass cancelled = cancelled into the RunManifest(...) constructor.
       - At the end of finally, runCatching { 
     Runtime.getRuntime().removeShutdownHook(hook) }
     so a normal exit doesn't leave the hook dangling.
       - Define private val SHUTDOWN_TIMEOUT = 30.seconds near the top of the
     file (or pull into a shared :utils constant — duplication of one line
     is fine for now).

     Hook shape (same in both Main.kt files):
     val rootJob = coroutineContext.job
     val shutdownHook = Thread {
         log.warn("Shutdown signal received; cancelling scrape gracefully (up to
      {})", SHUTDOWN_TIMEOUT)
         rootJob.cancel(CancellationException("Shutdown signal received"))
         runBlocking {
             val finished = withTimeoutOrNull(SHUTDOWN_TIMEOUT) { rootJob.join()
      }
             if (finished == null) log.warn("Graceful shutdown timed out after
     {}", SHUTDOWN_TIMEOUT)
         }
     }
     Runtime.getRuntime().addShutdownHook(shutdownHook)

     Why this works under Gradle's JavaExec

     The application plugin's run task forks a separate JVM for the scraper.
     Whether the user Ctrl-Cs the gradle wrapper, kills the gradle daemon, or
     Gradle terminates the JavaExec process on its own, the child JVM ultimately
     receives a shutdown signal — at which point our hook fires. Hooks run on
     fresh threads, so they can outlive the main thread's runBlocking long
     enough to drive cleanup.

     Verification

     1. ./gradlew build — compiles cleanly.
     2. Cancel mid-run: start ./gradlew :keep:run --args="full" with a
     sufficiently large workload (drop --limit), wait until the log shows
     "Issues progress: 25/N processed", press Ctrl-C. Confirm:
       - Log contains Shutdown signal received; cancelling scrape gracefully...
     followed by phase-end logs from the cancelled collectors.
       - data/manifests/Kotlin_KEEP-full-<ts>.json exists and has
     "cancelled": true, populated counts, and a sensible requests_made.
       - data/cache/etags.json, last-sync.json, and git-heads.json were
     updated (their mtime should be after the run started).
       - data/normalized/*.jsonl files have no partial lines (every line ends
     in a newline; wc -l matches the manifest's per-stream count).
     3. Resume: re-run with update. The run manifest's requests_304
     should be high (most fetched URLs unchanged) and the since cursor
     should pick up where the cancelled run left off.
     4. Timeout escape: artificially make a fetch hang (e.g., add a
     temporary delay(60_000) before a fetch) and Ctrl-C. Confirm the hook
     logs Graceful shutdown timed out after 30s and the JVM exits within
     ~30 seconds, with the run manifest still written reflecting whatever
     state was reachable.
     5. Hard kill: a second Ctrl-C during the graceful shutdown window
     bypasses the hook and JVM exits immediately. This is expected — document
     in the run-instructions that Ctrl-C is graceful, second Ctrl-C is force.

     ---
     Addendum: fix countListEndpoint fallback when GitHub omits rel="last"

     Context

     The probe in GithubClient.countListEndpoint reads the Link header to
     discover the total item count of a list endpoint (e.g. /issues?since=...).
     GitHub does not always include rel="last" — for /issues requests with
     the since query param specifically (and a handful of other filtered
     endpoints), GitHub returns only rel="next" for paginated multi-page
     responses.

     The current fallback when lastPageNumber returns null is the response
     body's array size. With per_page=1, that's always 1 — which is reported
     as the total. So in incremental (update) mode where since is set, every
     issues phase logs Issues progress: N/1 processed regardless of how many
     issues actually need fetching. Misleading.

     Approach

     Distinguish the two cases that produce a missing rel="last":

     1. Single-page result — there are 0 or 1 pages of results, no rel="next"
     either. The body's array size IS the true total. Return it.
     2. Multi-page result with last omitted — rel="next" is present even
     though last isn't. The total is genuinely unknown via this probe.
     Return null so the denominator in the log shows ?, which is honest,
     instead of a wrong 1.

     Paginator.nextLink(...) already exists and returns the rel="next" URL
     when present — reuse it.

     Critical files to be modified

     - utils/src/main/kotlin/dev/cse3000/gh/client/GithubClient.kt
       - In countListEndpoint, replace the single-line Elvis fallback:
       Paginator.lastPageNumber(resp.headers[HttpHeaders.Link])
         ?: (Jsons.compact.parseToJsonElement(resp.bodyAsText()) as?
     JsonArray)?.size
       - with explicit branching:
       val link = resp.headers[HttpHeaders.Link]
     Paginator.lastPageNumber(link)?.let { return@withPermit it }
     if (Paginator.nextLink(link) != null) return@withPermit null
     return@withPermit (Jsons.compact.parseToJsonElement(resp.bodyAsText()) as?
     JsonArray)?.size

     No other files change. The downstream callers in IssueAndPrCollector
     already treat null as "?" via denominator = limit ?: total ?: "?".

     Verification

     1. ./gradlew build — green.
     2. Incremental issues run with a non-trivial since cursor: re-run
     :keep:run --args="update" after the previous full scrape. The
     Issues phase starting (...) log should report total=null (instead
     of total=1); the progress log should read Issues progress: 25/?
     instead of Issues progress: 25/1.
     3. Full issues run (no since): unchanged — total=N from the real
     last page number.
     4. PRs and Discussions phases: unchanged. PRs probe omits since
     (the endpoint doesn't accept it) so last is always present.
     Discussions use GraphQL totalCount, unrelated to this code path.

     ---
     Addendum: shared SQLite database via sibling GitLab repo

     Context

     This scraper runs alongside other scrapers operated by 10 collaborator
     projects in the same GitLab group. They all populate a single shared SQLite
     database that lives in a separate repository under the same GitLab project,
      e.g.

     gitlab.tudelft.nl/cse3000-rq3/
       scrapers-keep-kep/         <- this repo (covers two projects: KEEP + KEP)
       scrapers-<other>/           <- collaborator repos
       proposals-db/               <- the shared sibling repo

     The shared schema is in db-schema.txt (preliminary). Key design:

     - Project(project_id INTEGER PRIMARY KEY, project_name, 
     enhancement_proposal_name, copyright)
     — there are 10 projects total. Each contributor is assigned one or more
     project IDs. This repo owns two: one for KEEP (Kotlin) and one for
     KEP (Kubernetes). Exact integer IDs are coordinated externally.
     - All proposal-scoped tables (Proposal, ProposalRevision, StageHistory,
     ProposalRevisionAuthor, RelatedProposal, Comment) carry project_id
     as part of their composite key. Each contributor owns the rows for their
     own project_ids and rewrites them on each push.
     - Global tables (Person, Organisation, PersonUsername, Affiliation)
     are shared across all projects: the same GitHub user appearing in
     multiple contributors' data should resolve to a single person_id.
     Identity is established by (PersonUsername.domain, 
     PersonUsername.username).

     We need two pipelines:

     1. Push: turn this scraper's data/normalized/*.jsonl into rows in the
     shared database for two project_ids, then get those rows into the
     sibling repo.
     2. Pull: get the latest shared database to a local SQLite file and run
     analysis from :rq3.

     User decisions (from clarifying questions):

     - The shared proposals.db SQLite file is committed to the sibling repo
     as the canonical artifact (binary in git).
     - Build/serve to consumers is a local script (no GitLab CI required).
     - The push pipeline produces files only — the researcher pushes manually
     (no JGit, no GitLab auth inside the build).

     Architecture

     ┌─ This repo
     ─────────────────────────────────────────────────────────────────┐
     │  :scraper :keep :kep        (existing)
        │
     │         │
        │
     │         ▼ writes data/normalized/*.jsonl
        │
     │  :loader (NEW)
        │
     │         │
        │
     │         ▼ writes loader/build/export/keep-kep/{schema,delete,data}.sql
        │
     │         │  + apply.sh
        │
     │  ┌──────┴──── researcher manually runs apply.sh
     ──────────────────────────┐ │
     │  │
      │ │
     │  ▼
      ▼ │
     │ Sibling repo (proposals-db) checkout: ~/.../proposals-db/proposals.db
     (.db updated)
     │  ◀── git pull / push ──▶
        │
     │  ▲
        │
     │  │ (researcher git pull, then SHARED_DB_PATH points at it)
        │
     │  ▼
        │
     │  :rq3
        │
     │         │
        │
     │         ▼ syncSharedDb copies proposals.db → rq3/data/shared/
        │
     │         │
        │
     │         ▼ analysis Kotlin code opens via sqlite-jdbc
        │
     └──────────────────────────────────────────────────────────────────────────
     ───┘

     The sibling repo is treated as an external dependency — both the
     producer (:loader) and consumer (:rq3) reference it by filesystem path
     through a SHARED_DB_PATH env var (or -PsharedDbPath=...). Researchers
     manage their sibling-repo checkout with normal git pull / git push; no
     gitlab credentials live inside this build.

     Sibling repo expectations (proposals-db)

     We don't own this repo, but the contract it must provide is:

     - schema/init.sql — collaborative DDL. All tables include a source column
     (or analogous discriminator) so each contributor's data can be deleted +
     re-inserted independently.
     - proposals.db — committed SQLite file, kept current with schema/init.sql.
     - scripts/init-db.sh — drops proposals.db and rebuilds it from scratch
     via schema/init.sql. Used when the schema changes incompatibly.
     - README.md — documents the contributor workflow (apply your inserts →
     commit
     → push) and reminds contributors to coordinate to avoid concurrent .db
     merge
     conflicts.

     This addendum doesn't need to build the sibling repo for them — it just
     specifies the contract :loader writes against and :rq3 reads from.

     :loader module (NEW)

     Goal: convert this scraper's normalized JSONL streams into a single SQL
     file that, when applied once to the shared proposals.db, populates two
     project IDs' worth of rows (one for KEEP, one for KEP).

     This is a one-shot push — no incremental updates. After this contributor
     applies their data.sql once, they're done. If a re-apply is ever needed
     (e.g., schema rebuild), the maintainer rebuilds proposals.db from
     db-schema.txt and every contributor re-applies their one-shot output.

     :loader
       src/main/kotlin/dev/cse3000/loader/
         SchemaModel.kt           # Kotlin types mirroring db-schema.txt
     (manually kept in sync)
         JsonlSource.kt           # streams from data/normalized/<stream>.jsonl
         IdAllocator.kt           # sequential ID generator within a
     contributor-assigned range
                                  # (e.g. for keepProjectId=3, person_ids start
     at 3_000_000)
         KeepMapper.kt            # JSONL streams (keep-*) → SchemaModel rows
     for the KEEP project_id
         KepMapper.kt             # JSONL streams (kep-*)  → SchemaModel rows
     for the KEP project_id
         SqlWriter.kt             # emits INSERT statements with sqlite-safe
     quoting
         Main.kt                  # CLI entry
       build.gradle.kts           # apply application plugin; depends on
     :scraper

     No PersonResolver, no -PsharedDb read. Person dedup is deferred to
     :rq3 post-processing. The loader simply allocates person_id values in
     a contributor-specific range so they don't collide with other contributors'
     allocations. :rq3 (or whatever consumer cares) can dedupe by
     (PersonUsername.domain, PersonUsername.username) later, merging duplicate
     Persons into one canonical row and updating FKs.

     ID allocation strategy: a small range per project keeps things simple.
     Convention: person_id ∈ [projectId × 1_000_000, (projectId+1) × 1_000_000).
     For a contributor with two project IDs (3 and 4), use the smaller as the
     base (person_id starts at 3_000_000). Same scheme for organisation_id
     and comment_id. With 10 projects total and at most ~tens of thousands of
     distinct authors per project, 1M slots is plenty of headroom.

     CLI shape:

     ./gradlew :loader:run \
        -PkeepProjectId=3 \
        -PkepProjectId=4 \
        [-PdataDir=data]                          # defaults to ./data
        [-Pout=loader/build/export/keep-kep]

     Output:

     loader/build/export/keep-kep/
       schema.txt                 # snapshot of the schema the loader was
     written against
                                  # (sanity check the maintainer can compare
     against the current .db)
       data.sql                   # all INSERTs in dependency order:
                                  #   1. Project (the two rows we own)
                                  #   2. Person, PersonUsername, Organisation,
     Affiliation
                                  #   3. Proposal
                                  #   4. ProposalRevision,
     ProposalRevisionAuthor
                                  #   5. StageHistory
                                  #   6. RelatedProposal
                                  #   7. Comment
       apply.sh                   # bash apply.sh /path/to/proposals.db
                                  #   sqlite3 $1 < data.sql
       apply.bat                  # Windows equivalent

     Mapping per JSONL stream (high level — exact SQL once schema is final):

     ┌────────────────────────────────────────┬────────────────────────────────┐
     │             JSONL streams              │          Target rows           │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │ keep-pulls.jsonl /                     │ Proposal(project_id,           │
     │ kep-yaml-revisions.jsonl               │ proposal_id, proposer_id,      │
     │ (proposal-bearing)                     │ topic, proposal_type)          │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │ keep-proposal-revisions.jsonl /        │ ProposalRevision,              │
     │ kep-yaml-revisions.jsonl +             │ ProposalRevisionAuthor         │
     │ kep-readme-revisions.jsonl             │                                │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │ Parsed kep.yaml status field across    │ StageHistory                   │
     │ revisions                              │                                │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │ keep-pulls.jsonl state changes         │ StageHistory                   │
     │ (timeline events)                      │                                │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │ *-issue-comments, *-pr-issuecomments,  │ Comment (with                  │
     │ *-pr-review-comments,                  │ comment_on_comment_id for      │
     │ *-discussion-comments,                 │ replies)                       │
     │ *-discussion-comment-replies           │                                │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │                                        │ Person, PersonUsername         │
     │ Author/login fields across all streams │ (allocated locally, dedup      │
     │                                        │ deferred to :rq3)              │
     ├────────────────────────────────────────┼────────────────────────────────┤
     │                                        │ RelatedProposal (parsed from   │
     │ Cross-references in proposal bodies    │ kep.yaml replaces/superseded   │
     │ (e.g. "Replaces #N")                   │ fields and from KEEP markdown  │
     │                                        │ front-matter)                  │
     └────────────────────────────────────────┴────────────────────────────────┘

     The Project row identity (project_name, enhancement_proposal_name,
     copyright) is set as a constants list in KeepMapper.kt / KepMapper.kt.

     Dependencies: stdlib + kotlinx-serialization-json (transitively via
     :scraper).
     No sqlite-jdbc needed in :loader — it never opens the shared db.

     Push workflow (manual, one-shot)

     # 1. Scrape (run scrapers to completion at least once)
     ./gradlew :keep:run --args="--mode=full"
     ./gradlew :kep:run  --args="--mode=full"

     # 2. Generate the one-shot SQL using assigned project ids
     ./gradlew :loader:run -PkeepProjectId=3 -PkepProjectId=4

     # 3. Pull the sibling repo and apply
     cd ~/projects/proposals-db && git pull
     bash ~/projects/this-repo/loader/build/export/keep-kep/apply.sh
     ./proposals.db

     # 4. Commit + push the modified .db
     git add proposals.db
     git commit -m "[keep-kep] one-shot import"
     git push

     Because the loader doesn't read the existing proposals.db, steps 2 and
     3 don't have to be tightly coupled — there's no race window. Other
     contributors can have pushed between your scrape and your apply. The only
     requirement is that the schema in the .db at apply time matches the schema
     the loader was written against (compare
     loader/build/export/keep-kep/schema.txt
     against the .db's sqlite_schema if in doubt).

     The apply.sh is regenerated under loader/build/... (a build-output path,
     gitignored in this repo) on every loader run.

     :rq3 consumer side

     Add two tasks and one Kotlin file:

     :rq3
       src/main/kotlin/dev/cse3000/rq3/
         SharedDb.kt          # opens proposals.db via sqlite-jdbc; exposes a
     Connection
         Analyses.kt          # research queries (filled in by RQ3 work)
         Main.kt              # entry point
       build.gradle.kts       # apply application plugin; add
     org.xerial:sqlite-jdbc

     Tasks:

     - :rq3:syncSharedDb — reads SHARED_DB_PATH env var (or -PsharedDbPath=...,
     default ../proposals-db/proposals.db), validates it exists, and copies
     it to rq3/data/shared/proposals.db. Plain Files.copy(...) — no JGit.
     - :rq3:run (provided by the application plugin) — runs analysis against
     rq3/data/shared/proposals.db. Fails fast with a clear message if the
     file is missing, telling the user to run syncSharedDb first.

     For the researcher's pull workflow:

     cd ~/projects/proposals-db && git pull
     cd ~/projects/this-repo
     ./gradlew :rq3:syncSharedDb -PsharedDbPath=../../proposals-db/proposals.db
     ./gradlew :rq3:run

     Or set SHARED_DB_PATH once in their shell profile and skip the -P flag.

     Why copy rather than read-in-place: keeps the sibling-repo checkout
     immutable
     (no SQLite WAL files appearing during analysis), and lets :rq3 cache a
     snapshot for reproducibility (re-running an analysis later against the same
     snapshot even if the sibling repo has been updated since).

     Critical files to be created

     - settings.gradle.kts — add include(":loader")
     - loader/build.gradle.kts (NEW) — applies application plugin, depends on
     :scraper
     - loader/src/main/kotlin/dev/cse3000/loader/Main.kt (NEW) — parses
     -PkeepProjectId, -PkepProjectId, -PdataDir, -Pout; fails fast
     with usage if either project id is missing
     - loader/src/main/kotlin/dev/cse3000/loader/SchemaModel.kt (NEW) — Kotlin
     data classes mirroring db-schema.txt (Project, Person, PersonUsername,
     Organisation, Affiliation, Proposal, ProposalRevision,
     ProposalRevisionAuthor,
     StageHistory, RelatedProposal, Comment)
     - loader/src/main/kotlin/dev/cse3000/loader/JsonlSource.kt (NEW) — generic
     JsonElement Flow over a JSONL file
     - loader/src/main/kotlin/dev/cse3000/loader/IdAllocator.kt (NEW) —
     sequential
     ID generator within [base, base + 1_000_000) for
     Person/Organisation/Comment;
     base is derived from the smaller assigned project_id × 1_000_000
     - loader/src/main/kotlin/dev/cse3000/loader/KeepMapper.kt (NEW) — maps
     keep-*.jsonl streams → SchemaModel rows for the KEEP project_id
     - loader/src/main/kotlin/dev/cse3000/loader/KepMapper.kt (NEW) — same for
     kep-*.jsonl
     - loader/src/main/kotlin/dev/cse3000/loader/SqlWriter.kt (NEW) — emits
     data.sql / apply.sh / apply.bat with sqlite-safe quoting (single-quote
     escape, ISO-8601 datetimes, NULL handling)
     - rq3/build.gradle.kts — apply application plugin, add sqlite-jdbc
     - rq3/src/main/kotlin/dev/cse3000/rq3/SharedDb.kt (NEW) — opens
     data/shared/proposals.db via JDBC, exposes a Connection
     - rq3/src/main/kotlin/dev/cse3000/rq3/Main.kt (NEW) — minimal entry point
     that opens the DB and prints sanity row counts per table; real analyses
     (and any optional Person dedup post-processing) come later
     - gradle/libs.versions.toml — add sqlite-jdbc entry (e.g. 3.45.x) for
     :rq3 only
     - README.md — add a "Sharing data with collaborators" section explaining
     the push/pull workflow above

     Operational notes

     - Concurrent commits to proposals.db will conflict (binary, no merge).
     Treat the sibling repo as a serialized resource — coordinate via Slack /
     whatever channel before pushing. Since the loader doesn't read the .db,
     there's no race window inside this contributor's flow; only the
     git push of the modified .db needs serialization across contributors.
     - Schema changes: db-schema.txt is coordinated separately. If it
     changes, every contributor updates their SchemaModel.kt, re-runs
     :loader:run, the sibling-repo maintainer rebuilds an empty proposals.db
     from the new schema, and each contributor re-applies their one-shot
     output once. After that round-robin, the .db is the new canonical state.
     - No idempotency-on-re-apply guarantee: re-running apply.sh against
     a .db that already contains this contributor's rows will fail on PRIMARY
     KEY violations (Proposal, ProposalRevision, etc.). That's intentional —
     this is a one-shot push. If a re-apply is genuinely needed, the
     contributor manually deletes their project_id rows first, or the
     maintainer rebuilds the .db from scratch.
     - Person ID range collision avoidance: each contributor's IdAllocator
     uses [smallestAssignedProjectId × 1_000_000, +1_000_000) for person_id,
     organisation_id, and comment_id allocations. With 10 projects and
     ~10⁶ slots per range, this is collision-free without coordination. If a
     contributor genuinely produces more than 1M Persons (unlikely), bump the
     multiplier to 10M.
     - Person dedup is not the loader's job. The same GitHub user appearing
     in two contributors' data will exist as two Person rows with different
     IDs after both contributors apply. :rq3 (or a future maintenance task)
     can dedupe by (PersonUsername.domain, PersonUsername.username) once
     all data is in. Flag to schema collaborators: add a UNIQUE(domain, 
     username) index to PersonUsername to make dedup-via-MERGE-and-FK-rewrite
     straightforward downstream.

     Verification

     1. ./gradlew :loader:build — module compiles cleanly.
     2. Synthetic push test: with a populated data/normalized/, run
     ./gradlew :loader:run -PkeepProjectId=3 -PkepProjectId=4. Confirm
     loader/build/export/keep-kep/ contains schema.txt, data.sql,
     apply.sh, apply.bat. Spot-check data.sql: project_id is 3 or 4
     on every project-scoped row; person_id values are all in
     [3_000_000, 4_000_000); timestamps are ISO-8601; FK ordering looks
     sane (Persons declared before their first reference).
     3. End-to-end apply: in a tmp dir,
     sqlite3 tmp.db < db-schema.txt && bash apply.sh tmp.db. Then
     sqlite3 tmp.db "SELECT COUNT(*) FROM Proposal WHERE project_id = 3" —
     matches the count of proposal-bearing PRs in keep-pulls.jsonl. Same
     for project_id=4 vs distinct proposals in kep-yaml-revisions.jsonl.
     SELECT COUNT(*) FROM Comment WHERE project_id IN (3,4) matches the
     sum of the relevant comment streams.
     4. Cross-contributor coexistence: simulate another contributor by
     inserting placeholder rows with project_id=5 and person_id in
     [5_000_000, 6_000_000) before running our apply. Then apply. No
     conflicts; their rows are untouched, our rows live alongside.
     5. Pull side: with SHARED_DB_PATH=path/to/tmp.db (or the equivalent
     project property), run ./gradlew :rq3:syncSharedDb — verify
     rq3/data/shared/proposals.db exists, matches by SHA-256, and
     ./gradlew :rq3:run prints sane row counts per table.

     ---
     Addendum: scrape GitHub user metadata for a manually-supplied login list

     Context

     The loader maps GitHub commenters / authors / committers into Person /
     PersonUsername rows (via the personByGHLogin map inside KeepMapper /
     KepMapper). Those rows currently carry only the login — no display name,
     no organisation, no profile blob. We want a separate one-off scrape step
     that
     takes a list of GitHub logins and fetches each user's full metadata via
     GET /users/{login}, emitting it as JSONL for later enrichment.

     User constraint: don't over-automate this. The list of logins is supplied
     manually (a plain text file). The fetch task is invoked manually, once. No
     auto-discovery from the loader, no auto-enrichment of the shared SQLite.
     The
     researcher decides what to do with users.jsonl afterwards (extend the
     loader to read it back and fill Person.full_name, push to a separate file
     for :rq3 post-processing, whatever).

     Approach

     A new standalone scrape entry point in :scraper that reuses every piece of
     existing infrastructure (GithubClient for ETag/rate-limit/retry/IOException
     handling, runScrape for shutdown hook + flush + manifest). The collector
     itself is ~30 lines because GitHub's /users/{login} endpoint needs no
     pagination — one request per login, one row out.

     $env:GITHUB_TOKEN = "ghp_..."
     ./gradlew :scraper:runUsers -Pinput=path/to/logins.txt

     Input file format: one GitHub login per line, blank lines and lines
     starting
     with # ignored. Example:

     # generated by hand from PersonUsername rows
     JakeWharton
     chrisbanes
     elizarov

     Output: <dataDir>/normalized/users.jsonl — one line per successfully
     fetched
     user, each row is the raw /users/{login} response (login, id, name,
     company,
     location, email-if-public, bio, blog, twitter_username, public_repos,
     followers, following, created_at, updated_at, etc.) with the standard
     _scraped_at / _repo (set to literal "users") meta fields the rest of
     the scraper adds.

     Default dataDir for this task is data/users/ so the user-fetch state
     (ETag store, raw cache, manifest) is isolated from your main KEEP/KEP
     scrape
     caches and can be deleted independently if you want a fresh run.

     Critical files to be created / modified

     - scraper/src/main/kotlin/dev/cse3000/gh/scraper/UsersCollector.kt (NEW)
       - Constructor: (client, sink, owner = "users", repo = "users", repoTag = 
     "users") — owner/repo are placeholders for the meta fields; the API path is
      fixed as /users/{login}.
       - suspend fun fetch(login: String): hits client.getJson("/users/$login"),
      emits to users.jsonl with mapOf("login" to login) meta. Catches "404 Not
     Found" thrown by fetchWithRetry and logs a WARN; doesn't abort the whole
     batch.
       - suspend fun run(logins: List<String>, limit: Int? = null): iterates
     with progress logging every 25; respects optional limit for smoke tests.
     - scraper/src/main/kotlin/dev/cse3000/gh/cli/UsersMain.kt (NEW)
       - Reads --input=path (required) and --dataDir=path (default data/users)
     from system properties (forwarded by the Gradle task from -P).
       - Parses the input file: filter blanks + # lines, trim, dedupe, sort.
       - Wraps the collector call in runScrape(...) for the standard scaffolding
      (shutdown hook, periodic flush, manifest, stop-file watcher). The body
     block constructs UsersCollector(...) and calls run(logins, limit). Repo
     slug in the manifest can be "github-users" to keep manifests distinct from
     regular scrape runs.
       - Reuses parseScrapeArgs(args, "scraper-users", allowProposals = false)
     to share the --limit / --mode flag parsing — --mode=full|update here just
     toggles whether ETag-cached 304s are honored, so update is the sane default
      for re-runs.
     - scraper/build.gradle.kts (EDIT)
       - Add a runUsers JavaExec task with mainClass = 
     "dev.cse3000.gh.cli.UsersMainKt".
       - Forward input and dataDir Gradle properties as system properties (same
     pattern the existing :loader:run config uses).
     - README.md (EDIT)
       - Add a short "Scraping user metadata" subsection under "Running the
     scrapers" pointing at :scraper:runUsers with the input-file format, a
     one-line example, and a note that the file is supplied manually (the loader
      can optionally drop one alongside data.sql if the researcher wants to wire
      that up — single line in SqlWriter).

     Why a new task and not a --include=users phase

     The other phases are repo-scoped: they all hit /repos/{slug}/... endpoints
     and slot into the existing IssueAndPrCollector / DiscussionsCollector /
     CommitsCollector shape. User metadata is identity-scoped (/users/{login})
     and the input is a manually-curated list, not a repo. Folding it into the
     phase enum would muddy ScrapePhase (most phases require --repo=; this one
     doesn't). A dedicated runnable task keeps the surface clean.

     What's intentionally NOT done

     - No automatic discovery of logins from the loader's output. The loader can
     emit logins.txt if you add one line to SqlWriter (write distinct
     PersonUsername.username values where domain == "github.com"), but
     that's optional and out of scope for this addendum.
     - No automatic enrichment of proposals.db Person rows. The output is raw
     JSONL; downstream (a :rq3 analysis step or a future loader extension)
     decides if/how to merge.
     - No GraphQL bulk lookup. GitHub's node(id: "MDQ6VXNlcjE=") would batch
     ~100 users per query and save quota, but for ~hundreds-to-low-thousands of
     users it's not worth the codegen complexity. REST /users/{login} is
     one request each, ETag-cached on rerun.

     Verification

     1. ./gradlew :scraper:build — green.
     2. Smoke test: write tmp/logins.txt with 3 known logins (e.g.,
     JakeWharton, elizarov, nonexistent-login-9999999).
     ./gradlew :scraper:runUsers -Pinput=tmp/logins.txt -PdataDir=tmp/users.
     Expect: tmp/users/normalized/users.jsonl has 2 rows; the 404 login
     logs a single WARN and is skipped; manifest at
     tmp/users/manifests/github-users-update-*.json reports requests_made=3,
     errors=[] (the 404 is handled, not fatal).
     3. Re-run: same command. Manifest reports requests_304=2 and zero
     new normalized rows (the third login still 404s and is skipped quickly).
     4. Cancel test: feed a long list (e.g., 500 logins), run, hit Ctrl-C
     (or touch tmp/users/STOP). Manifest has cancelled=true; users.jsonl
     has the partial set of rows fully formed (no truncated lines), and the
     ETag cache is populated for the logins that were fetched before
     cancellation.

     ---
     Addendum: nest this repo inside a GitLab "monorepo" via git submodules

     Context

     This whole project moves into a GitLab "monorepo" — but a
     git-submodule-based
     one rather than a single flattened repo. Each contributor's project remains
     its own standalone GitLab repository with its own history; the monorepo
     root
     is a separate GitLab project that wires every contributor in as a submodule
     and additionally holds the shared artifacts (db-schema.sql and, depending
     on team decision, proposals.db).

     This contributor's repo keeps its current name —
     CSE3000_RQ3-KEEP-KEP (matching rootProject.name in
     settings.gradle.kts) — and lives as a submodule directory of the same name
     inside the monorepo root.

     Layout after the move:

     <monorepo-root>/                       # standalone GitLab project, its own
      .git
       .gitmodules                          # registers every contributor
     submodule
       db-schema.sql                        # shared, tracked by the monorepo
     .git
       proposals.db                         # shared SQLite (committed binary,
                                            #   tracked by the monorepo .git;
                                            #   optional — see "Open items")
       CSE3000_RQ3-KEEP-KEP/                # this repo, as a submodule (own
     .git)
         settings.gradle.kts                # rootProject.name unchanged
         keep/  kep/  scraper/  loader/  rq3/
         data/  build/  ...
       <other-contributor-A>/                # submodule
       <other-contributor-B>/                # submodule
       ...

     Three goals to satisfy at once:

     1. Migrate without disruption — current Gradle commands (./gradlew 
     :keep:run,
     :loader:run, :rq3:syncSharedDb, etc.) keep working from inside
     CSE3000_RQ3-KEEP-KEP/ with the same arguments. Existing repository
     history is preserved (submodules don't rewrite history).
     2. Open just this folder in IntelliJ — sibling contributor folders aren't
     indexed, their Gradle modules don't show up, our :run configurations
     stay clean. The submodule model makes this even cleaner: a contributor
     can clone just their own repo and never touch the monorepo root, and
     they can still clone the monorepo with --recurse-submodules when they
     want the whole picture.
     3. Read/write the parent on demand — read the shared db-schema.sql
     (loader needs the snapshot); write the shared proposals.db (apply our
     data.sql). All other parent-dir touches happen via plain shell + git
     commands at the monorepo-root level.

     Recommended approach

     Keep CSE3000_RQ3-KEEP-KEP/ as its own self-contained Gradle build — its
     own settings.gradle.kts, its own wrapper, its own rootProject.projectDir,
     its own GitLab project, its own .git/. The monorepo root is not a
     Gradle build; it's a directory of git submodules plus a few shared files.
     This
     is the simplest possible model and the one that makes "open just this
     folder"
     trivial in any IDE: IntelliJ opens CSE3000_RQ3-KEEP-KEP/, sees
     settings.gradle.kts there, treats that as the project root, and never
     touches sibling submodules. The git VCS root IntelliJ detects is this
     submodule's .git/, not the monorepo root's — so the IDE's "Changes" view
     shows only this contributor's diffs, and commits go to this repo's history.

     The few places that point outside this folder (the schema snapshot, the
     shared-DB sync target) are already parameterized via -P… properties or env
     vars. We add a single new convention — a monorepoRoot Gradle property,
     default
     .. — and route all outward-facing default paths through it. That way the
     defaults "just work" inside the monorepo, and a standalone clone outside
     the
     monorepo overrides one property and is back to the current behavior.

     Why submodules instead of a flattened monorepo?

     A flattened monorepo (one giant .git/ covering everyone) would force every
     contributor's IDE to walk the whole tree on git operations and would mix
     unrelated commit histories into one log. Submodules keep each contributor's
     repo independent — independent push/pull, independent issues/MRs on GitLab,
     independent visibility — while still letting the monorepo root pin a
     coherent
     "snapshot of the whole project" via the recorded submodule SHAs.

     Why not a Gradle composite build at the monorepo root?

     includeBuild from a monorepo-root settings.gradle.kts would let you run
     ./gradlew :CSE3000_RQ3-KEEP-KEP:keep:run from the monorepo root, but it has
     real costs:

     - IntelliJ pointed at the monorepo root would index every contributor's
     source — the exact "open just this folder" property we want to keep.
     - Gradle daemons get bigger; config-cache invalidation across contributors
     is
     a footgun.
     - Cross-contributor dependencies aren't a use case here — each scraper is
     independent.

     Skip composite. Each contributor submodule is its own standalone Gradle
     build.

     Why not move the IntelliJ project root up?

     If you opened the monorepo root in IntelliJ, every contributor's submodule
     would get auto-imported (or at minimum land in scope-search), and our
     finely-tuned :run configurations would coexist with sibling contributors'
     configs. Keep IntelliJ pointed at CSE3000_RQ3-KEEP-KEP/.

     Why not hard-code ../db-schema.sql?

     Two reasons. One, contributors who clone just this repo (the submodule, by
     itself) would break. Two, the lookup chain is already the shape we want
     (parameterized + ordered fallbacks) — adding the monorepo root path is one
     more entry in a list that already exists.

     Concrete changes

     1. loader/src/main/kotlin/dev/cse3000/loader/Main.kt

     Today (lines 16–19):

     val schemaSource = listOf(
         Paths.get("db-schema.sql"),
         Paths.get("loader/db-schema.sql"),
     ).firstOrNull { java.nio.file.Files.exists(it) }

     Becomes:

     val schemaSource = sequenceOf(
         System.getProperty("schemaPath")?.let(Paths::get),     // explicit 
     override
         System.getProperty("monorepoRoot")?.let { Paths.get(it,
     "db-schema.sql") },
         Paths.get("..", "db-schema.sql"),                      // monorepo 
     default
         Paths.get("db-schema.sql"),                            // legacy / 
     standalone
     ).filterNotNull().firstOrNull { java.nio.file.Files.exists(it) }

     -PschemaPath=… is the explicit-override knob (mirrors -PsharedDbPath=…);
     -PmonorepoRoot=… is the conventional knob; the bare ../db-schema.sql and
     ./db-schema.sql paths are zero-config defaults.

     2. loader/build.gradle.kts

     Forward schemaPath and monorepoRoot properties to the JVM as system
     properties (same pattern as -PsharedDbPath in rq3/build.gradle.kts):

     tasks.named<JavaExec>("run").configure {
         listOf("keepProjectId", "kepProjectId", "dataDir", "out",
                "schemaPath", "monorepoRoot").forEach { name ->
             project.findProperty(name)?.let { systemProperty(name,
     it.toString()) }
         }
     }

     (Verify the existing forwarding shape — extend the existing forEach list
     rather than duplicating it.)

     3. rq3/src/main/kotlin/dev/cse3000/rq3/SharedDb.kt

     Today (lines 21–25):

     fun resolvePath(): Path {
         val override = System.getProperty("sharedDbPath")?.takeIf {
     it.isNotBlank() }
         if (override != null) return Paths.get(override).toAbsolutePath()
         return Paths.get("data", "shared", "proposals.db").toAbsolutePath()
     }

     Becomes:

     fun resolvePath(): Path {
         System.getProperty("sharedDbPath")?.takeIf { it.isNotBlank() }
             ?.let { return Paths.get(it).toAbsolutePath() }
         System.getProperty("monorepoRoot")?.takeIf { it.isNotBlank() }
             ?.let { return Paths.get(it, "proposals.db").toAbsolutePath() }
         // Synced/copied default — same as today.
         return Paths.get("data", "shared", "proposals.db").toAbsolutePath()
     }

     This makes :rq3:run open ../proposals.db directly when monorepoRoot is
     set, with no syncSharedDb step needed. Synced-snapshot mode (the existing
     data/shared/proposals.db) is still the fallback for users who prefer
     reproducibility.

     4. rq3/build.gradle.kts

     Add monorepoRoot to the list of forwarded properties so the override flows
     through to both :rq3:run and (cosmetically) :rq3:syncSharedDb. The
     syncSharedDb task itself stays as-is — it copies from
     -PsharedDbPath=... regardless, and an explicit override is the right model
     for that task.

     5. New convention: default monorepoRoot to ..

     Add to buildSrc/src/main/kotlin/kotlin-jvm.gradle.kts, after the existing
     workingDir = rootProject.projectDir block:

     tasks.withType<JavaExec>().configureEach {
         workingDir = rootProject.projectDir
         // Make the monorepo root visible to every JavaExec'd main; default is 
     the
         // parent of this contributor folder. Override with -PmonorepoRoot=... 
     or
         // unset to disable (e.g., when this repo is cloned standalone).
         val monorepoRoot = (project.findProperty("monorepoRoot") as? String)
             ?: rootProject.projectDir.parentFile?.absolutePath
         monorepoRoot?.let { systemProperty("monorepoRoot", it) }
     }

     This single block makes every :run task in every module see
     -DmonorepoRoot=…. Loader and rq3 read it; other modules ignore it.

     6. Move db-schema.sql to the monorepo root

     The schema is currently checked in at <this-repo>/db-schema.sql. After the
     migration:

     - The canonical copy lives at <monorepo-root>/db-schema.sql.
     - The local copy in this repo is deleted (not duplicated). The loader's
     fallback chain finds ../db-schema.sql automatically.
     - Standalone-clone use (this repo without the monorepo) requires the
     contributor to drop a copy at db-schema.sql or pass -PschemaPath=….
     Document this in the README's "Standalone use" subsection.

     7. README updates

     Add a new top section right after "Prerequisites":

     ▎ Monorepo layout (git submodules)
     ▎
     ▎ This repo (CSE3000_RQ3-KEEP-KEP) is consumed in two ways:
     ▎ - Standalone: cloned on its own (e.g., git clone <this-repo-url>).
     ▎ Works as before; provide db-schema.sql locally or via -PschemaPath=….
     ▎ - As a submodule of the team monorepo: the monorepo root holds the
     ▎ shared db-schema.sql (and optionally proposals.db) one level up,
     ▎ plus a .gitmodules entry pointing at this repo. Clone the monorepo
     ▎ with git clone --recurse-submodules <monorepo-url> to get everything,
     ▎ or git submodule update --init CSE3000_RQ3-KEEP-KEP to pull just this
     ▎ submodule.
     ▎
     ▎ Either way, open CSE3000_RQ3-KEEP-KEP/ in IntelliJ, not the monorepo
     ▎ root, so sibling contributors' submodules aren't pulled into indexing. 
     ▎ All
     ▎ Gradle commands run from inside this folder.

     Replace "All commands assume you're at the repo root" with "All commands
     run
     from the CSE3000_RQ3-KEEP-KEP/ folder". Update the loader/push and
     :rq3:syncSharedDb examples to show -PsharedDbPath=../proposals.db.

     8. .gitignore

     No change. data/, build/, .idea/* patterns all stay folder-local.
     db-schema.sql and proposals.db at the monorepo root are tracked by the
     monorepo root's .git/ (a separate repo from this submodule's .git/);
     this folder's .gitignore doesn't need to acknowledge them, and they don't
     appear in this repo's diffs because they live above this submodule's
     worktree.

     Critical files to be modified

     - loader/src/main/kotlin/dev/cse3000/loader/Main.kt — schema lookup chain
     - loader/build.gradle.kts — forward schemaPath, monorepoRoot
     - rq3/src/main/kotlin/dev/cse3000/rq3/SharedDb.kt — monorepoRoot fallback
     - rq3/build.gradle.kts — forward monorepoRoot
     - buildSrc/src/main/kotlin/kotlin-jvm.gradle.kts — default monorepoRoot
     system property for every JavaExec
     - db-schema.sql (deleted from this folder, recreated at monorepo root)
     - README.md — add "Monorepo layout" + workflow updates

     No new Gradle tasks, no new modules, no new dependencies.

     Migration steps

     One-time, by whoever sets up the monorepo

     1. Create the monorepo GitLab project (e.g.,
     cse3000-rq3/proposals-monorepo).
     Initial commit holds: empty .gitmodules, the canonical db-schema.sql,
     and (optionally) proposals.db.
     2. For each contributor repo, run from inside the monorepo root:
     git submodule add <contributor-repo-url> <contributor-repo-name>.
     This contributor goes in as
     git submodule add <this-repo-url> CSE3000_RQ3-KEEP-KEP. Commit the
     resulting .gitmodules + submodule pointers.

     One-time, in this repo (this contributor's PR)

     1. Apply the code changes (loader schema lookup, SharedDb fallback, gradle
     forwarding, kotlin-jvm.gradle.kts default monorepoRoot).
     2. Delete db-schema.sql from this repo (the canonical copy is at the
     monorepo root). Update README per "README updates" above.
     3. Push the branch, merge — and from the monorepo root:
     cd CSE3000_RQ3-KEEP-KEP && git pull && cd .. && git add 
     CSE3000_RQ3-KEEP-KEP && git commit -m "bump CSE3000_RQ3-KEEP-KEP submodule"
     to advance the submodule pointer.

     Per-contributor, after migration

     - Daily work (only this repo matters): clone this submodule alone,
     cd CSE3000_RQ3-KEEP-KEP, run scrapers as before. ./gradlew :loader:run
     picks up db-schema.sql from the standalone fallback (or -PschemaPath)
     if the monorepo isn't checked out around it.
     - Push to shared DB: clone the monorepo with --recurse-submodules.
     From <monorepo-root>/CSE3000_RQ3-KEEP-KEP/, run :loader:run; the
     schema is read from ../db-schema.sql automatically. Apply with
     bash build/export/keep-kep/apply.sh ../proposals.db. Then
     cd .. && git add proposals.db && git commit && git push at the
     monorepo-root level.
     - Pull shared DB for analysis: cd <monorepo-root> && git pull
     refreshes proposals.db. Then either
     ./gradlew :rq3:run from CSE3000_RQ3-KEEP-KEP/ (uses monorepoRoot
     default to open ../proposals.db directly), or
     ./gradlew :rq3:syncSharedDb -PsharedDbPath=../proposals.db for a copy.

     Verification

     1. Standalone-clone smoke: clone this repo to a tmp dir without the
     monorepo wrapper; drop a copy of db-schema.sql at the repo root or pass
     -PschemaPath=.... ./gradlew :loader:run succeeds with the same output
     counts as today. (Proves we didn't break the standalone path.)
     2. Monorepo smoke: from
     <monorepo-root>/CSE3000_RQ3-KEEP-KEP/, run ./gradlew :loader:run 
     -PkeepProjectId=3 -PkepProjectId=4. Confirm the loader's log line shows
     ../db-schema.sql was picked from the parent dir.
     3. rq3 direct-open: ./gradlew :rq3:run from
     CSE3000_RQ3-KEEP-KEP/ inside the monorepo picks up ../proposals.db
     via the monorepoRoot default. With -PsharedDbPath=… set, that takes
     precedence.
     4. IntelliJ open-the-subfolder: open CSE3000_RQ3-KEEP-KEP/ in
     IntelliJ; the Project view shows only this submodule's modules; no
     sibling contributor submodules appear. Git VCS root resolves to this
     submodule's .git/. Run configurations for :keep:run, :loader:run,
     :rq3:run resolve and execute.
     5. Override knob: ./gradlew :loader:run -PmonorepoRoot=/some/other/path 
     -PkeepProjectId=3 -PkepProjectId=4 — confirms the override path takes
     effect and the schema is read from the explicit location.
     6. Submodule update flow: from monorepo root, after pushing a change in
     this repo, run cd CSE3000_RQ3-KEEP-KEP && git pull then cd .. and
     verify git status shows the submodule pointer needs to be advanced.
     git add CSE3000_RQ3-KEEP-KEP && git commit && git push advances it.

     Open items deferred to implementation

     - Shared proposals.db location: the earlier "shared SQLite via sibling
     GitLab repo" addendum assumed proposals.db lives in a sibling repo
     (proposals-db/). With the submodule monorepo, proposals.db can live
     directly at the monorepo root and be tracked by the monorepo's .git/,
     or it can stay in a separate proposals-db repo and become another
     submodule. This addendum supports both: keep using -PsharedDbPath=…
     for a sibling-repo / nested-submodule checkout, or rely on monorepoRoot
     defaulting to .. for the simpler "directly in monorepo root" case. The
     README should pick one as the documented default once the team decides.
     - Pre-commit hook on the monorepo root: optional. A hook that warns when
     db-schema.sql is touched (so contributors notice the cross-cutting
     change) is a 10-line shell script, separate from this plan.
     - CI: out of scope per earlier guidance ("local script, no GitLab CI").
     If CI is reintroduced later, it'd run ./gradlew check per contributor
     submodule via a matrix — same as the standalone case.

● Plan approved. Implementing now.

  Read 1 file (ctrl+o to expand)

● Update(loader\build.gradle.kts)
  ⎿  Added 1 line, removed 1 line
      16  // Forward selected -P<name>=<value> properties to the JVM as s
          ystem properties so Main.kt can read them.
      17  tasks.named<JavaExec>("run").configure {
      18      enableAssertions = true   // make `assert(...)` calls actua
          lly fire so data-cleanliness checks aren't no-ops.
      19 -    listOf("keepProjectId", "kepProjectId", "dataDir", "out").f
         -orEach { name ->                                               
      19 +    listOf("keepProjectId", "kepProjectId", "dataDir", "out", "
         +schemaPath", "monorepoRoot").forEach { name ->                 
      20          project.findProperty(name)?.let { systemProperty(name, 
          it.toString()) }
      21      }
      22  }

● Update(loader\src\main\kotlin\dev\cse3000\loader\Main.kt)
  ⎿  Added 10 lines, removed 3 lines
      13          ?: error("Pass -PkepProjectId=N (your assigned project_
          id for KEP)")
      14      val dataDir = Paths.get(System.getProperty("dataDir") ?: "d
          ata")
      15      val outDir: Path = Paths.get(System.getProperty("out") ?: "
          build/export/keep-kep")
      16 -    val schemaSource = listOf(                                 
      16 +    // Schema source resolution, in priority order:            
      17 +    // 1. -PschemaPath=<path> — explicit override (mirrors -Psh
         +aredDbPath in :rq3).                                           
      18 +    // 2. -PmonorepoRoot=<path>/db-schema.sql — when this repo 
         +is a submodule of the monorepo.                                
      19 +    //    The convention plugin (kotlin-jvm.gradle.kts) default
         +s `monorepoRoot` to the parent of                              
      20 +    //    rootProject.projectDir, so a clone-as-submodule layou
         +t works zero-config.                                           
      21 +    // 3. db-schema.sql in the JVM workingDir — for standalone 
         +clones without a monorepo wrapper.                             
      22 +    val schemaSource = sequenceOf(                             
      23 +        System.getProperty("schemaPath")?.let(Paths::get),     
      24 +        System.getProperty("monorepoRoot")?.let { Paths.get(it,
         + "db-schema.sql") },                                           
      25          Paths.get("db-schema.sql"),
      18 -        Paths.get("loader/db-schema.sql"),                     
      19 -    ).firstOrNull { java.nio.file.Files.exists(it) }           
      26 +    ).filterNotNull().firstOrNull { java.nio.file.Files.exists(
         +it) }                                                          
      27  
      28      val keepBaseId = keepProjectId.toLong() * 1_000_000L
      29      val kepBaseId = kepProjectId.toLong() * 1_000_000L

● Update(rq3\src\main\kotlin\dev\cse3000\rq3\SharedDb.kt)
  ⎿  Added 16 lines, removed 11 lines
       7  import java.sql.DriverManager
       8  
       9  /**
      10 - * Opens the synced shared SQLite database. Default location is
      11 - * `<repo-root>/data/shared/proposals.db` (populated by `:rq3:s
         -yncSharedDb`).                                                 
      12 - * The `:rq3:run` task is configured to use the repo root as it
         -s working                                                      
      13 - * directory so that the relative `data/shared/proposals.db` re
         -solves to                                                      
      14 - * the unified data dir shared by all the scrapers.            
      15 - *                                                             
      16 - * Override at runtime with `-PsharedDbPath=...` (forwarded to 
         -the JVM as                                                     
      17 - * `-DsharedDbPath=...`) — useful for analyses that want to poi
         -nt straight at                                                 
      18 - * a sibling-repo checkout without copying.                    
      10 + * Opens the shared SQLite database. Resolution priority:      
      11 + *   1. `-PsharedDbPath=...` — explicit override.              
      12 + *   2. `-PmonorepoRoot=.../proposals.db` — when this repo is a
         + submodule of the                                              
      13 + *      monorepo and `proposals.db` sits next to `db-schema.sql
         +` at the monorepo root.                                        
      14 + *      The convention plugin defaults `monorepoRoot` to the pa
         +rent of `rootProject.projectDir`.                              
      15 + *   3. `<repo-root>/data/shared/proposals.db` — the synced/cop
         +ied snapshot                                                   
      16 + *      written by `:rq3:syncSharedDb`. The `:rq3:run` task's w
         +orking directory                                               
      17 + *      is `rootProject.projectDir`, so this relative path reso
         +lves to the                                                    
      18 + *      unified data dir shared by all the scrapers.           
      19   */
      20  object SharedDb {
      21      fun resolvePath(): Path {
      22 -        val override = System.getProperty("sharedDbPath")?.take
         -If { it.isNotBlank() }                                         
      23 -        if (override != null) return Paths.get(override).toAbso
         -lutePath()                                                     
      22 +        System.getProperty("sharedDbPath")?.takeIf { it.isNotBl
         +ank() }                                                        
      23 +            ?.let { return Paths.get(it).toAbsolutePath() }    
      24 +        System.getProperty("monorepoRoot")?.takeIf { it.isNotBl
         +ank() }                                                        
      25 +            ?.let {                                            
      26 +                val candidate = Paths.get(it, "proposals.db")  
      27 +                if (Files.exists(candidate)) return candidate.t
         +oAbsolutePath()                                                
      28 +            }                                                  
      29          return Paths.get("data", "shared", "proposals.db").toAb
          solutePath()
      30      }
      31  

● Update(rq3\build.gradle.kts)
  ⎿  Added 1 line, removed 1 line
      19  
      20  // Forward -PsharedDbPath / -PrunReport flags to the JVM.
      21  tasks.named<JavaExec>("run").configure {
      22 -    listOf("sharedDbPath").forEach { name ->                   
      22 +    listOf("sharedDbPath", "monorepoRoot").forEach { name ->   
      23          project.findProperty(name)?.let { systemProperty(name, 
          it.toString()) }
      24      }
      25  }

● Update(buildSrc\src\main\kotlin\kotlin-jvm.gradle.kts)
  ⎿  Added 11 lines
      35  // instead of `<module>/data/`, and the loader can read everyth
          ing from one place without a copy step.
      36  tasks.withType<JavaExec>().configureEach {
      37      workingDir = rootProject.projectDir
      38 +                                                               
      39 +    // Make the monorepo root visible to every JavaExec'd main 
         +as `-DmonorepoRoot=...`.                                       
      40 +    // Default: the parent of rootProject.projectDir — i.e. whe
         +n this repo is checked out                                     
      41 +    // as a git submodule of `<monorepo-root>/`, the loader and
         + rq3 transparently see                                         
      42 +    // `../db-schema.sql` and `../proposals.db`. Override with 
         +`-PmonorepoRoot=...`.                                          
      43 +    // For a standalone clone outside the monorepo, the parent 
         +dir simply won't contain                                       
      44 +    // those files and the standard fallbacks (loader's `db-sch
         +ema.sql`, rq3's synced copy)                                   
      45 +    // kick in unchanged.                                      
      46 +    val monorepoRoot = (project.findProperty("monorepoRoot") as
         +? String)                                                      
      47 +        ?: rootProject.projectDir.parentFile?.absolutePath     
      48 +    monorepoRoot?.let { systemProperty("monorepoRoot", it) }   
      49  }

● Now let me verify the loader still finds the schema (currently db-schema.sql
  is at this folder's root — falls back to entry #3):

  Listed 1 directory (ctrl+o to expand)

● Bash(./gradlew :loader:run -PkeepProjectId=3 -PkepProjectId=4 2>&1 | tail -8)
  ⎿  18:06:01.706 INFO  d.c.loader.KeepMapper - Resolver counts: GH logins=
     453, emails=0, names=50, total Person rows=503
     18:06:01.706 INFO  d.c.loader.KeepMapper - Proposal status mapping: ra
     … +74 lines (ctrl+o to expand)
  ⎿  (timeout 3m)

  Read 1 file, listed 1 directory (ctrl+o to expand)

● Update(README.md)
  ⎿  Added 24 lines, removed 1 line
      26  export GITHUB_TOKEN=ghp_...
      27  ```
      28  
      29 +## Monorepo layout (git submodules)                            
      30 +                                                               
      31 +This repo (`CSE3000_RQ3-KEEP-KEP`) is consumed two ways:       
      32 +                                                               
      33 +- **Standalone**: `git clone <this-repo-url>`. The schema lives
         + at                                                            
      34 +  `db-schema.sql` in this repo's root and the loader picks it u
         +p automatically.                                               
      35 +- **As a submodule of the team monorepo**: the monorepo root ho
         +lds the shared                                                 
      36 +  `db-schema.sql` (and optionally `proposals.db`) one level up,
         + plus a                                                        
      37 +  `.gitmodules` entry pointing at this repo. Clone the monorepo
         + with                                                          
      38 +  `git clone --recurse-submodules <monorepo-url>` to get everyt
         +hing, or                                                       
      39 +  `git submodule update --init CSE3000_RQ3-KEEP-KEP` to pull ju
         +st this                                                        
      40 +  submodule. The Gradle convention plugin sets `-DmonorepoRoot=
         +<parent dir>`                                                  
      41 +  for every `:run` task, so the loader and `:rq3` discover `../
         +db-schema.sql`                                                 
      42 +  / `../proposals.db` automatically.                           
      43 +                                                               
      44 +Either way, **open `CSE3000_RQ3-KEEP-KEP/` in IntelliJ** (not t
         +he monorepo                                                    
      45 +root), so sibling contributors' submodules aren't pulled into i
         +ndexing.                                                       
      46 +                                                               
      47 +Override knobs (any `:run` task):                              
      48 +- `-PschemaPath=path/to/db-schema.sql` — explicit schema locati
         +on for the loader.                                             
      49 +- `-PsharedDbPath=path/to/proposals.db` — explicit shared-DB lo
         +cation for `:rq3`.                                             
      50 +- `-PmonorepoRoot=path/to/monorepo` — override the auto-detecte
         +d monorepo root.                                               
      51 +                                                               
      52  ## Running the scrapers
      53  
      31 -All commands assume you're at the repo root.                   
      54 +All commands run from inside `CSE3000_RQ3-KEEP-KEP/`.          
      55  
      56  ### KEEP and KEP (the named scrapers)
      57  

● Update(README.md)
  ⎿  Added 6 lines, removed 5 lines
      309  # 2. Generate SQL (replace 3 / 4 with your assigned project id
           s)
      310  ./gradlew :loader:run -PkeepProjectId=3 -PkepProjectId=4
      311  
      312 -# 3. Apply to the sibling repo's committed proposals.db       
      313 -cd ~/projects/proposals-db && git pull                        
      314 -bash ~/projects/this-repo/loader/build/export/keep-kep/apply.s
          -h ./proposals.db                                              
      312 +# 3. Apply to the shared proposals.db. In the monorepo-submodu
          +le layout, that                                               
      313 +#    .db lives at the monorepo root one level up:             
      314 +bash build/export/keep-kep/apply.sh ../proposals.db           
      315  
      316 -# 4. Commit + push the modified .db                           
      316 +# 4. Commit + push the modified .db at the monorepo-root level
      317 +cd ..                                                         
      318  git add proposals.db
      318 -git commit -m "[keep-kep] one-shot import"                    
      319 +git commit -m "[CSE3000_RQ3-KEEP-KEP] one-shot import"        
      320  git push
      321  ```
      322  

● Update(README.md)
  ⎿  Added 22 lines, removed 15 lines
      339  ### Pull and analyse in `:rq3`
      340  
      341  ```sh
      342 -# 1. Pull the latest sibling repo                             
      343 -cd ~/projects/proposals-db && git pull                        
      342 +# 1. Pull the latest monorepo-root state (advances proposals.d
          +b via submodule                                               
      343 +#    pointer commit too, depending on team workflow)          
      344 +cd <monorepo-root> && git pull                                
      345  
      345 -# 2. Sync the shared db into this repo's :rq3 module          
      346 -cd ~/projects/this-repo                                       
      347 -./gradlew :rq3:syncSharedDb -PsharedDbPath=../../proposals-db/
          -proposals.db                                                  
      348 -                                                              
      349 -# 3. Run analyses (initially: print sanity row counts per tabl
          -e)                                                            
      346 +# 2a. Direct-open mode: :rq3 reads ../proposals.db via the aut
          +o-detected                                                    
      347 +#     monorepoRoot. No copy step needed.                      
      348 +cd CSE3000_RQ3-KEEP-KEP                                       
      349  ./gradlew :rq3:run
      350 +                                                              
      351 +# 2b. Snapshot mode: copy the .db into the local data dir for 
          +reproducibility.                                              
      352 +./gradlew :rq3:syncSharedDb -PsharedDbPath=../proposals.db    
      353 +./gradlew :rq3:run    # opens data/shared/proposals.db        
      354  ```
      355  
      353 -`syncSharedDb` copies the .db into `rq3/data/shared/proposals.
          -db` (gitignored).                                             
      354 -`:rq3:run` opens it via `sqlite-jdbc`. You can also point `:rq
          -3` at the                                                     
      355 -sibling-repo checkout directly without copying via            
      356 -`-PsharedDbPath=path/to/proposals.db` on `:rq3:run`.          
      356 +`syncSharedDb` copies the .db into `data/shared/proposals.db` 
          +(gitignored).                                                 
      357 +`:rq3:run` opens it via `sqlite-jdbc`. Resolution priority for
          + the shared DB:                                               
      358 +`-PsharedDbPath` (explicit) > `<monorepoRoot>/proposals.db` (i
          +f it exists) >                                                
      359 +`<repo-root>/data/shared/proposals.db` (the synced snapshot). 
      360  
      361  ### Schema is `db-schema.sql`
      362  
      360 -The collaborative schema lives in `db-schema.sql` at the repo 
          -root. The loader's                                            
      361 -`SchemaModel.kt` data classes mirror it; if the schema changes
          -, update both                                                 
      362 -together. The loader's per-stream → SQL mapping (`KeepMapper.k
          -t` / `KepMapper.kt`)                                          
      363 -is TBD                                                        
      363 +The collaborative schema lives at **`<monorepo-root>/db-schema
          +.sql`** when this                                             
      364 +repo is checked out as a submodule, or **`<repo-root>/db-schem
          +a.sql`** when                                                 
      365 +cloned standalone. The loader's `SchemaModel.kt` data classes 
          +mirror it; if                                                 
      366 +the schema changes, update both together. Resolution priority 
          +for the loader:                                               
      367 +`-PschemaPath` (explicit) > `<monorepoRoot>/db-schema.sql` > `
          +db-schema.sql`                                                
      368 +in the JVM working directory.                                 
      369  
      370 +The loader's per-stream → SQL mapping (`KeepMapper.kt` / `KepM
          +apper.kt`) is TBD                                             
      371 +                                                              
      372  ## Build
      373  
      374  ```sh

● Final smoke: confirm the code paths still compile and run via standalone
  fallback, plus verify the override knob works:

● Bash(mkdir -p /tmp/fakemono && cp
      C:/Users/kajus/IdeaProjects/CSE3000_RQ3-KEEP-KEP/db-schema.sql
      /tmp/fakemono/db-schema.sql && head -1 /tmp/fakemono/db-schema.sql;…)  ⎿  -- Generated primary keys (person_id, organisation_id, comment_id star
     t at their project_id*1_000_000)
     ----
     … +3 lines (ctrl+o to expand)
  ⎿  (timeout 2m)

● Bash(diff -q /tmp/fakemono/db-schema.sql C:/Users/kajus/IdeaProjects/CSE3000_R
      Q3-KEEP-KEP/build/export/keep-kep/schema.sql; rm -rf /tmp/fakemono)
  ⎿  (No output)

● Override works (file matched the explicit -PschemaPath).

  Summary of changes implemented:

  - buildSrc/src/main/kotlin/kotlin-jvm.gradle.kts: every JavaExec now sets
  -DmonorepoRoot=<parent of rootProject.projectDir> by default, overridable via
  -PmonorepoRoot=….
  - loader/src/main/kotlin/dev/cse3000/loader/Main.kt: schema resolution chain
  is now -PschemaPath → ${monorepoRoot}/db-schema.sql → ./db-schema.sql. The
  legacy loader/db-schema.sql lookup was dropped (it never existed in practice).
  - loader/build.gradle.kts: forwards schemaPath and monorepoRoot.
  - rq3/src/main/kotlin/dev/cse3000/rq3/SharedDb.kt: resolution chain is
  -PsharedDbPath → ${monorepoRoot}/proposals.db (only if it exists) →
  data/shared/proposals.db.
  - rq3/build.gradle.kts: forwards monorepoRoot.
  - README.md: new "Monorepo layout (git submodules)" section right above
  "Running the scrapers"; loader/push and :rq3 workflows updated to show the
  in-monorepo paths (bash apply.sh ../proposals.db, direct-open vs snapshot
  mode).

  db-schema.sql is not deleted from this repo yet — that's the migration step
  you'd take after the monorepo is set up at .. with its own canonical copy.
  Until then, the standalone fallback keeps the existing flow intact.

  Verified: standalone run still works (loader picks db-schema.sql from repo
  root, output schema.sql matches), and -PschemaPath=/tmp/fakemono/db-schema.sql
   correctly takes priority.

✻ Churned for 11m 4s